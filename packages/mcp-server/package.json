{
  "name": "@interlock/mcp-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "MCP server exposing Interlock findings and advice to coding agents. Thin adapter over the daemon API — no analysis logic lives here.",
  "license": "AGPL-3.0-only",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@interlock/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.4.0"
  }
}
