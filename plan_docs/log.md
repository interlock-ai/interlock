# Log

Short entries: done, decided, blocked. Newest first.

---

## 2026-09-05 — a third review, three real bugs

- **Re-measured after the mark-race fix, because the published numbers were taken against the bug.** A change landing during a hash was dropped until the 60s ceiling, so the reasonable expectation was a bimodal tail whose slow half fell outside a 60s window and biased p50 and p95 low. It did not move: 1902/2315 against 1881/2424, same 87 samples. The reason is the workload — writes land every 200ms, so a clobbered mark is replaced by the next one within a fifth of a second, and the ceiling is never reached. The bug's real victim is an _isolated_ edit arriving during a capture with nothing following it, which waits up to a minute and which continuous rewriting cannot expose. The published latencies stand for continuous editing; they were never evidence about that tail either way.

- **A worktree switching branches published nothing, and mislabelled what it kept.** Identity was the worktree alone, so `checkout -b` over work in progress produced the same tree, the dedupe read it as no change, and `rememberOn` stamped the new branch with the old branch's snapshot. Nothing downstream ever heard the new branch existed. Reproduced red before fixing. The remembered identity is the worktree _and_ the branch in it now.
- **One branch's failure starved every branch after it, on every pass.** A capture that throws — a worktree removed mid-pass will — escaped the loop. Held rather than swallowed: the loop runs to the end and the failure is rethrown after it, because a pass that could not snapshot something did not succeed and a broken runner behind a quiet log line goes unnoticed for a week. Both properties are tested, and the second was found by the first attempt breaking the existing merge-base test.
- **A mark arriving during a hash was discarded.** `changed: false` was written on the way out, so a signal describing content the walk had already passed over was lost until the ceiling. The flag is cleared before the walk and whatever landed during it survives.
- **Accepted deliberately, and now pinned: a restart announces everything again.** The remembered identity is in memory. Republishing content a consumer may already hold is the safe direction — a restarted consumer holds nothing either — and it costs one hash per worktree, once. Persisting the last tree would buy that back for the price of a cache in the schema, which is a trade to make when something needs it.
- **Two of that review's findings were already fixed and one was wrong.** The unknown-every-pass flooding went in the previous round; single-flight has five tests rather than none, and the double-run window it describes resolves correctly in both orderings, which those tests cover. Both readings were of an older revision.

## 2026-09-05 — two review findings, both real

- **An unreadable worktree was announced on every pass, for ever.** The `dirty === null` path published unconditionally: a worktree on an unmounted volume writes roughly seven hundred identical rows an hour into a log retention only trims by age. The comment justified the _first_ one — silence would read as "nothing changed" — and that argument covers the transition, not the repetition. Worse, `moved` in the sweep already treats null to null as no change, so two modules held opposite answers about the same state. What was last said about a worktree is now a value, `unknown` or a tree, and only a change to it is published.
- **A test encoded that bug and had to be rewritten**, per hard rule 5 rather than patched around: it asserted "unknown twice running is still unknown twice", which is precisely the flooding. The property worth keeping — that a worktree coming back is announced even when its content is unchanged — was already covered by its own test.
- **One branch with an unresolvable default branch failed the entire repository, every pass.** `resolveDefaultBranch` guesses: `origin/HEAD` can name a branch nobody fetched, and a detached head falls back to `main` whether one exists or not. `mergeBase` raises rather than answering null for a revision it cannot resolve — deliberately, so a pair is never dropped in silence — and that raise propagated out of the branch loop, so `all()` marked the repository failed on every pass and it was never reconciled again. Reproduced before fixing: `reconcile THREW: git merge-base failed`. It is now a snapshot with no diff and a warning naming the branch, which is not silent and does not cost the repository.

## 2026-09-05 — the watcher's numbers

Measured on this laptop with `pnpm exec tsx scripts/watcher-bench.ts`: 10,000 files, three worktrees, a 5s sweep, 60s of continuous rewriting. The machine was not quiet — the baseline sat at 5–7% — so every CPU figure is a difference against a baseline run of the same workload with the watcher off, and the noise floor is roughly ±3%.

- **idle** — one pass over an unchanged repository costs **375–505ms** across runs, of which almost all is one `git status` per worktree. At the 5s cadence the benchmark used, that is **0.68% of this ten-core machine** and about 10% of one core. Meeting 2% _of one core_ needs 25s between passes; against a machine-wide reading the budget is already met. The budget does not say which, and the two answers differ by an order of magnitude — worth settling before the composition picks a cadence.
- **Recommended sweep cadence: 30s**, which is what the composition should start with. A pass costs 375–505ms, so 30s is 1.3–1.7% of one core and 0.13–0.17% of a ten-core machine — under 2% on either reading, which retires the question of which reading the budget meant rather than settling it. Nothing is traded away for it: real edits arrive as filesystem signals and land at the debounce ceiling in about two seconds, commits and checkouts write `HEAD` and `refs` so the ref watcher signals those too, and the sweep only backstops what the platform failed to report — which the 60s recapture ceiling already bounds. Revisit on a repository much larger than ten thousand files, where a pass past ~600ms takes the headroom away.
- **active** — **9.5%** machine-wide while three worktrees are rewritten continuously, with per-change latency **p50 1881ms, p95 2424ms** over 87 samples.

- **The latency is the debounce, not the work.** Writes land every 200ms against a 250ms quiet period, so no batch ever goes quiet and every one runs to the 2s ceiling. p50 sitting just under it says the pipeline adds tens of milliseconds to a wait that the constant decides. M2's budget should be read as "the ceiling, plus a little", and lowering the ceiling moves it directly.
- **Measured the wrong thing twice before measuring the right one.** Timing from the newest write reported 117ms — how stale a snapshot is when it lands, not how long a change waited; it is timed from the oldest unanswered write now. And sampling CPU could not see the idle cost at all: at a 30s cadence the difference came out _negative_, because the watcher's cost is below what a machine running anything else can resolve. Timing a pass directly is precise and needs no quiet machine.
- **The measurement changed the design.** A pass hashed every worktree it saw, costing **1442ms**, which needs a 72s cadence to fit 2% of a core — no sane interval. The task's own wording says a snapshot follows _a debounce_, so hashing is now driven by a filesystem signal naming the worktree, with a 60s ceiling for the events macOS drops. That is what took a pass from 1442ms to 504ms.
- **Decided, with the numbers in hand: no per-`status` timeout below the runner's 30s default.** A `status` on ten thousand files measures about 150ms, so the default is two hundred times the observed cost. A shorter bound would only ever fire on something genuinely pathological, and the failure it risks — a slow but working worktree reported unreadable — is the worse one.
- **`changeset.computed` is gone.** Declared, never published, never consumed, and it would have been a second name for the moment `branch.snapshot` records, carrying strictly less: no tree identity, which is the whole point.
- **The pipeline fills `DirtyState.snapshotId`,** which nothing did before — discovery always wrote `null`, so `contentIdentity` collapsed to the head alone and two different uncommitted states of one commit read as identical content. It is written on a deduplicated pass too, from the cache, because the sweep re-lists with `null` every time.

## 2026-09-05

- **Found and fixed a shipped bug: an unreadable worktree was reported clean.** `git worktree list --porcelain` prints `prunable gitdir file points to non-existent location`, with no lock, for a directory it merely cannot enter — the same word it uses for one that is really gone. Discovery's filter dropped it, the branch was listed with no worktree, and `cleanState()` claimed no uncommitted work. That is the one answer nothing ever revisits, and `tryDirtyState`'s own comment forbids it. `prunable` means three things and only the filesystem separates the first from the third, so the filter now keeps an entry whose path still exists. Found while building a fixture for the snapshot pipeline, not by reading the code.
- **`chmod 000` is the reversible way to make a worktree unreadable**, and it is a different fixture from deleting the directory: git reports the same `prunable`, but the work is still there. Deleting is irreversible, which is why the recovery case had no test until now.

## 2026-09-04

- **A repository registered by anything but its canonical root was announced as new on every pass.** `openUserRepo` resolves a subdirectory, a symlink or a linked worktree to the main worktree, and that is what `upsertRepo` stores — but the lookup used the caller's argument, so it never matched. Two consequences, and the second is the worse one: `repo.discovered` republished on a timer into an append-only log, and `describeOrKeep` left with no stored row to fall back on, which turns a malformed override file from "warn and keep the last good config" into "fail every pass". The fixture built canonical paths throughout, so nothing caught it.
- **The single-flight guard had a door in it: it keyed on the caller's spelling.** Reconciling one repository as `/repo` and as `/repo/src` produced two overlapping passes and two announcements — the failure the guard exists to prevent, reached another way, and reachable by design because the timer sweeps configured paths while a signal names the canonical worktree. Only git can say those spellings are one worktree, so a second caller gets past a synchronous check before it can be recognised; the pass now claims the canonical root as well, once it knows it, and releases both. Releasing both matters as much as claiming: one left behind hands a settled pass to every later sweep and the repository is never looked at again.
- **An assertion that could not fail:** `expect(later).not.toBe(Promise.resolve())`, which mints a fresh promise on every call, so it was true whatever the code did. Compared against the pass that actually ran now. Caught by review rather than by mutation — a mutation only fails a test that could have failed.
- **The spelling guard is two halves that pin opposite orderings, and one test could only ever reach one of them.** Claiming the canonical root matters when the _other_ spelling starts first; joining from inside the body matters when the canonical one did. Each mutation survived while the test happened to construct the other ordering — fixing one moved the survivor rather than removing it. Both directions are tested now, through a harness that parks the first pass immediately after `openUserRepo` resolves, which is the instant the claim has or has not happened.
- **Slowing git does not make two sweeps overlap where it matters, and the tests that claimed it did were measuring nothing.** Git calls are the only real yield point in a pass: the branch loop awaits the store, which is synchronous underneath, so a second pass parked on a timer cannot resume inside it. Whichever pass reaches the loop first runs it to completion, and the event count comes out the same whether the guard is there or not — three mutations on the spelling join survived against a test whose comment asserted the opposite. Both concurrency tests now count how many passes actually did the work, through a proxy on the store, which depends on how many passes ran rather than on how they interleaved.
- **The single-flight test passed by luck, which mutation testing caught and the suite could not.** Three concurrent `reconcile` calls produced one `branch.appeared` even with the guard removed: whether the passes actually interleave depends on how far apart their git calls land, so the count was right by accident. It is asserted on promise identity now — a second call for a repository already running returns the same promise — with the behavioural version kept but driven through a runner that delays every git call, so the passes genuinely overlap.
- **The sweep runs one pass per repository at a time.** The timer and a filesystem signal are two triggers on the same work: two overlapping passes both read the stored branches before either writes, both see every branch as new, and the appearance is announced twice. The store converges — upserts reconcile — but the log is append-only and does not. A second request joins the pass in flight rather than queueing behind it, because it would read the same git state and find nothing new to say.
- **`moved` now includes `worktreePath`.** It had been justified as "compared on exactly what the event conveys", which was the wrong test: the consumer downstream is a watcher holding a watch on that directory, and a branch that moved has to be re-targeted. The event still cannot carry the new path, so what it learns is to re-read rather than what to read — which is enough.
- **Checked and not real: a review reported `reason: 'deleted'` hardcoded with the `ignored` helper never called.** That is verbatim the mutation this suite runs as "every disappearance called deleted", and it is caught. The tree was read while a mutation pass had it patched — the second review to do so, after the one that poisoned a commit the same way.
- **Recorded, not acted on.** A rename is a delete and an appearance, so it takes the branch's pairs, change sets and findings with it; that matches git's own semantics and the doctrine that findings are prunable, but it means an agent renaming a work-in-progress branch resets its pair history. Repositories still have no deletion path — one removed from the config, or whose directory is gone, fails or persists forever — which is the other half of the store round's "no delete side". Nothing sets `causedBy` yet, though the schema carries it. All three want deciding where they are consumed rather than here.

- **`branch.disappeared` meant two things.** A branch added to `ignoreBranches` mid-session is deleted and announced with the same event as one deleted from git — but it still exists, and the task asked for it to stop being _reported_, not to be forgotten. On replay the two were indistinguishable, so an exclusion read as a destruction. A `reason` of `deleted` or `ignored` now says which, decided by re-running the same short-name match `listBranchRefs` uses. Added while nothing has persisted one of these; this is the third wire format this milestone that was cheap to widen only because it was still empty.
- **The disappearance is published before its row is deleted, and the order is the point.** Either order can fail between the two steps, but only this one repeats itself: a delete that fails leaves the branch to be reported again next pass, while an event that fails after the delete is a hole nothing can fill — the branch is then absent from git _and_ from the store, so no later sweep can notice it left.
- **Written down rather than left to be rediscovered:** a store write and its event are two steps with no transaction around them, so a failure between them costs an announcement. That is a hole in the log, not in the state — the store is reconciled against git every pass and never rebuilt from the log, so what is lost is traceability for one change rather than the change itself.
- **Two costs on the path task 3 has to measure.** `reconcile` loaded the whole `repos` table to find one row, which `all()` made quadratic in the number of repositories, and `root_path` is already UNIQUE — a `getRepoByPath` is one indexed lookup. And `openUserRepo` ran twice per repository per sweep, a `rev-parse` and a `worktree list` each; the handle is opened once and passed through now.

- **`BranchUpdated.dirty` could not say "unknown", and that had to change before the sweep could publish anything honest.** It was typed `boolean`, so an unreadable worktree would have been reported as clean — undoing one layer out the exact distinction the store schema was built to keep, in a log that is never re-derived. Widened to `boolean | null`. Nothing has persisted one of these events yet, so changing the wire format cost nothing; in a month it would have cost a migration.
- **The store grew its deletion path here, where the task said it would be needed.** `deleteBranchRef` takes the branch's merge pairs and change sets with it by cascade, and a test pins that rather than trusting the schema — dropping `ON DELETE CASCADE` from `change_sets` leaves the suite green otherwise.
- **Branches reconcile under the id the store returned, not the one just minted.** Every sighting mints a fresh ULID, so writing branches against the described repo rather than the stored one orphans every row on the foreign key. Mutation-pinned, because nothing about the code reads as wrong.
- **The mid-session config re-read needed no mechanism at all** — `describeRepo` already re-reads `.interlock.json` on every call, so it works by construction. What needed writing was the failure path: a malformed file keeps the last good config, and only when there is one. A first sighting with a broken file is refused outright, because storing the defaults would watch what the file may have been excluding.
- **14 mutations, 12 caught on the first pass, and both survivors were tests that passed for the wrong reason.** Folding `null` into `false` in the dirty flag survived because the only test for it asserted the _store_ held null — which the store's own suite already pinned — while nothing asserted the _event_ carried it. And "a first sighting with a malformed file is refused" asserted only that something threw: swallow the parse error and the repo goes to the store with no id, whose constraint violation rejects too, for a reason no caller could act on. The first is now a readable-to-unreadable transition, which a flag that cannot see `null` reports as no change at all; the second asserts the code.

## 2026-08-30

- **`no-unreachable` was off, and the reason is a real gap rather than a config slip.** An unreachable `return` survived lint entirely: typescript-eslint's `eslint-recommended` turns the core rule off on the grounds that TypeScript checks it, and TypeScript only does when `allowUnreachableCode` is `false`, which was never set. Setting it in `tsconfig.base.json` catches the case at typecheck — which is where the toolchain expects the check to live, and where an editor shows it — rather than re-enabling a rule the preset deliberately disabled.
- **Deleted a lookup table instead of pinning it.** A map from debounce key to `{kind, worktreePath}` had to be cleaned up on `unwatch` or it outlived every target, and the fix for that was unpinnable without exposing internals. The key already encodes both, and a NUL cannot appear in a path, so parsing it back removes the state, the leak and the need for a test at once.
- **A debounce ceiling below its quiet period is refused rather than clamped.** Clamping meant the combination silently became something else, and the clamp itself could not be observed. Refusing makes the misconfiguration loud, and the code is shorter for it.
- **Considered and deliberately left alone:** `gitignorePatterns` treats a `\!`-escaped pattern as literal rather than as an escaped `!`, and `unwatch` given a non-canonical spelling of an already-deleted path leaks its entry. Both fail toward watching more than necessary, which costs a discarded signal rather than a lost change, and neither is reachable without a repository written to trip it.
- **Checked a review's headline finding and it does not hold.** It reported the `REF_FILES` allowlist as absent from the ref callback — dead code that let index churn through as ref signals. The filter is there and always has been, and a mutation removing it fails a test. The same review flagged a `keys` leak that had already been fixed. Both readings were of an older revision.
- **Its supporting claim was also wrong, and measuring it was worth doing.** The argument was that the churn test passes vacuously because a `git status` straight after a commit has a fresh stat cache and never writes the index. Measured on macOS: that `status` emits `index.lock`, `index`, `index` — named, not unnamed — so the filter is what keeps them from becoming ref signals, and the test discriminates. The suggested replacement is still the better test and was taken: `git add` writes the index unconditionally, so it cannot pass by producing no event at all.
- **And the trailing-space claim, checked rather than argued:** git ignores trailing spaces in a `.gitignore` line unless they are backslash-quoted, so `foo ` ignores `foo` and not `foo `. Trimming matches git; there is no divergence to fix.
- **Fixed, and these were real.** The `.git` filter matched only at the worktree root, so a submodule or vendored checkout leaked its internal churn as worktree paths; it matches at any depth now. `.gitignore` was read once at watch time and never again, so a repository that started ignoring `node_modules` mid-session kept burning events until something re-watched it; it is re-read when the file changes. A `maxWaitMs` below `waitMs` was ignored until the second push, because only the reschedule clamped. A git directory that is not there produced ref signals that silently never arrived, which is indistinguishable from a quiet repository, and is now logged.
- **Written down rather than left implicit:** degradation is target-wide even when one watcher of several failed — a watch budget is exhausted machine-wide, so the survivors are on borrowed descriptors — and nothing re-arms it except `unwatch` then `watch`, which a sweep does when it re-lists.
- **Measured, and worth knowing: a deleted and re-created subdirectory keeps delivering.** That is the classic recursive-watch hole, and macOS FSEvents self-heals it. Now pinned, so the day it stops being true on some platform it is a red test rather than a silence.

- **The watcher is a signal source, not an event publisher.** Two reviews reached this independently and it matches what the wire format already said: `worktree.changed` carries a `branchRefId`, and a path does not identify a branch — a worktree can be checked out onto another branch between the write and the lookup. Resolving identity in the watcher would record it at the one moment it cannot be known, which is the same mistake the store avoids by deriving `sessionId` on read. The sweep resolves once, after the debounce. `causedBy` needs no new mechanism for this: the sweep publishes `worktree.changed` once identity is known, and `EventBus` already threads `#currentCause` through dispatch, so a snapshot raised during that dispatch inherits it.
- **Measured the debounce floor instead of picking one.** macOS delivers a burst of 20–50 writes over ~55ms, with gaps of 43–50ms _between_ events. A debounce under that splits one save into several signals. The first test used 40ms and failed correctly — two signals was the right answer for that window, and the test's premise was what was wrong. The production default of 250ms now carries the measurement as its justification.
- **Fixed a real bug the degraded path exposed:** poll ticks were being pushed through the debouncer, so a poll interval shorter than the debounce restarted the quiet period on every tick and nothing escaped until the ceiling. A poll tick is not activity to coalesce, it is "time to look", and it is emitted directly now. Only visible because the fallback got a test.
- **Two error paths are platform-dependent and only a stub could reach them.** macOS goes quiet when a watched root is deleted; Linux reports `ENOENT`. The mutation removing the `ENOENT` branch survived on this machine for exactly that reason — the branch is never entered here. Driving the error through the injected factory pins both halves: a missing path closes that watch quietly, and any other error degrades to polling. Polling a directory that is never coming back would burn a timer forever.
- **`fs.watch` is injectable,** which is what makes the OS-refusal path testable at all — an exhausted inotify budget cannot be produced on demand. It is a kernel boundary, so the definition of done sanctions mocking it.
- **`.gitignore` is honoured, with two stated limits:** nested files are not read, and a file using `!` is dropped whole rather than partly applied. A negation rescues a path this matcher would exclude, and excluding a file that changed loses a change while watching one git ignores costs a discarded signal. The asymmetry decides it.
- **A linked worktree needs both git directories.** Its own git dir holds `HEAD` but no `refs/`; those are in the common dir. Watching only the first misses every commit, only the second misses every checkout, so `WatchTarget` takes `commonDir` and the test drives a real linked worktree.
- **A mutation harness poisoned a commit, and the fix is the harness.** A mutation replacing `paths: values.filter(...)` with `paths: values` was live in the working tree when the watcher was committed, so the commit carried the mutated source together with the test asserting the correct behaviour — CI caught it, which is the system working, but only after the fact. Two causes: the harness restored files by rewriting a remembered string rather than asking git, and it gave no sign that the tree was unsafe to commit while it ran. It now refuses to start on a dirty tree, restores with `git checkout --`, restores on interrupt, and fails loudly if the tree does not come back clean.
- **A batch holding one unnamed event is unnamed as a whole.** Filtering the marker out left the signal reporting only the paths that happened to have names, which is narrower than the truth in a module whose contract says empty means "ask git, never nothing changed" — a consumer scoping a status to those paths would miss whatever the unnamed event was. The pure-unnamed case was pinned; the mixed one was not.
- **14 mutations, 14 caught.** Two rounds were needed: the first caught 7 of 12, and all five survivors were real holes — index churn was asserted to produce no worktree signal but not to produce no _ref_ signal; the unwatch test cancelled before the platform had delivered anything, so there was no batch to cancel; a duplicate `watch()` coalesced into the same signal and hid the leaked watcher; a deleted worktree closed cleanly either way; and the unnamed-event path was unreachable without the stub.
- **Corrected: there is no startup replay, and the root event is not a startup artifact.** An earlier entry here said macOS delivers one signal when a watch starts because FSEvents replays existing entries, and that it costs one reconciliation. Re-measured against a tree left quiet for a second before watching: 800ms of silence produced **no events at all**. The first measurement had created a directory microseconds before `watch()` returned, so FSEvents was delivering something that had genuinely just happened. What does happen, ongoing rather than once, is that a metadata change on the watched directory — `utimes`, `chmod` — is reported as that directory's _own_ basename; creating a file or a directory at the root does not do it. Through the watcher that became a path naming a file inside the worktree that does not exist. It is now reported as unnamed rather than dropped, because an entry really called that would otherwise be lost, and "ask git" costs a status where guessing costs a change.

- **Split the Watcher task into three, because it was three.** It carried a path-aware glob matcher, filesystem and ref watching, a per-worktree debounce, a reconciliation sweep, a `.interlock.json` re-read, failing-repo containment, a store deletion API that does not exist, a `branch.snapshot` event that is not in the vocabulary, and the CPU and latency numbers M2's budget is built on. Its four **Done when** clauses already fell into three groups, and the `branch.snapshot` one cannot be demonstrated until the pipeline exists — so a single change could not be reviewable and could not be green in stages. Every constraint moved; none was dropped.
- **One glob scan, two policies, and it lives in `shared`.** `config.ignore` is documented as applying to ChangeSet extraction as well as watching, and `core` cannot import from `daemon`, so the matcher went to `shared` beside the existing pattern caps rather than into the watcher. `discovery.ts` lost its private copy of the scan and imports it; the pathological-pattern test there still passes, which is what proves the move changed no behaviour.
- **Checked our path semantics against real git rather than against memory of `.gitignore`.** Measured with `git check-ignore`: `src/*.ts` excludes `src/a.ts` and keeps `src/deep/x.ts`; a bare `node_modules` excludes at any depth; `dist/` excludes what is under it. All three match. Two do not, deliberately — git keeps a _file_ named `build` under `build/`, and keeps `a` itself under `a/**`, excluding only the contents. Both distinctions need a stat on a path that may already be gone by the time the event naming it is read, and a directory node is not a change worth watching either way.
- **The milestone's own example was slightly wrong and the difference is real anyway.** It said the path intuition breaks on `src/*` matching `src/deep/file.ts`; git excludes that file too, via the directory. The case that actually separates the two matchers is `src/*.ts`, which the branch matcher matches against `src/deep/x.ts` and the path matcher does not — that is the pair the test pins.
- **8 mutations, 7 caught.** The survivor is the whole-pattern length cap in `matchesPath`: the per-segment cap inside `matchesGlob` already decides every result, so the outer one is an early-out that uniquely bounds only the number of segments — which no assertion on a result can distinguish. Kept, and the test now claims only what is true of it.
- **Measured before planning:** `branch.snapshot` is absent from `packages/shared/src/events`, so the pipeline task adds a wire format rather than consuming one. Recursive `fs.watch` on macOS also emits an event naming the watched directory itself, which is not a change inside it and would otherwise read as one.

## 2026-08-29

- **Every entity now round-trips as a whole object, and that is deliberate rather than tidy.** A schema generated from the models — drizzle's real selling point — catches a model that gains a field with no column to hold it, at build time. Hand-written SQL cannot. Whole-object equality on the read path is the substitute: simulated three model fields arriving without columns and each one turns a test red. Branch refs, sessions and merge pairs were asserting field-at-a-time and would have missed it.
- **Reviewed the migration design as a base rather than as a diff, and none of it is a one-way door.** `.ts` to `.sql` is mechanical; `user_version` to a checksum ledger is migration 002, provided no migration was ever edited before then; hand-written to generated can start from 001 as a baseline. What is actually irreversible is the schema shape — what is a column and what is JSON — because every future migration has to preserve it. Recorded so the worry lands on the right thing.
- **`openStore` was tightening directories it did not create.** It chmodded the parent of the database path to 0700 unconditionally, which is right for Interlock's own data dir and wrong for the exported function that takes any path — a database put in a home or a shared directory would silently change that directory for everything else using it. `mkdirSync(recursive: true)` returns the first path it created or `undefined`, which is exactly the signal: set the mode on a directory this call created, warn about one that was already loose. The old test asserted the wrong behaviour (0755 in, 0700 out) and now asserts both halves.
- **`BEGIN IMMEDIATE` in `#transaction` too,** not only in the migration runner. `upsertFinding` and `prune` both write, so both had the same stale-snapshot exposure, and the `busy_timeout` comment was describing a wait that would not have happened.
- **Checked the claim that `db.isTransaction` should be replaced by self-tracked state, and it holds up as it is.** Measured both edges: after a statement fails inside `exec` it reports `true`, and after a `COMMIT` that fails on a deferred constraint it also reports `true`, with `ROLLBACK` then succeeding. The real defect nearby was different — a rollback that threw would replace the error that caused it. Both rollbacks now keep the original.
- **Checked the ULID monotonicity claim, and it is wrong.** A review put the event-ordering tests at 83% likely red on the theory that `ulid()` is not monotonic. Measured over the burst those tests actually create: 620 ids, 618 of the 619 consecutive pairs minted inside the same millisecond, strictly sorted and all unique. `bumpRandom` carries in base 32 rather than over raw bytes, which is the hazard being guessed at, and `ids.test.ts` already pins it. No sequence column, no monotonic mint.
- **Deferred with a home rather than left silent:** the store has no deletion path, so a branch that stops existing keeps its row, its pairs and its change sets — and `prune` keeps each branch's newest change set, so retention never reaches them. Written into the watcher task, which is what notices a branch is gone. `advice` is never pruned either; that arrives with M7, which is the first thing to write a row there.
- **Enumerated columns are validated on read.** An unrecognised verdict or finding status was flowing through an unchecked cast as though an analyzer had produced it — the opposite of "when a check is unsure it says nothing". A `oneOf` reader beside `text`/`num` refuses it. The sets come from the models themselves: `AgentKind`, `RunStatus`, `AnalyzerKind`, `AnalyzerVerdict`, `Severity` and `FindingStatus` are now derived from `as const` arrays the way `LogLevel` already was, so the list and the union cannot drift. CHECK constraints would have done it in the schema, but under the append-only rule adding an analyzer kind would then need a table rebuild.
- **JSON columns are parsed, not validated,** and the module doc says so now rather than claiming every read is checked. Validating each would be a schema library, and the shapes are the models this process wrote.
- **`PRAGMA foreign_keys = ON` was a no-op** — `node:sqlite` enables them by default. Replaced with `enableForeignKeyConstraints: true` at construction, which pins the default in one place instead of looking load-bearing. Removing that line survives mutation and always will: a pinned default is not detectable by construction. Setting it to `false` is caught, by the rejection tests and the cascade ones.
- **Fixed a migration race, and the reported mechanism was not the real one.** A review said two openers of a fresh store both read `user_version = 0` and the second dies on `table repos already exists`. It cannot: every statement in 001 is `IF NOT EXISTS`, so re-application is harmless — which is the property the task asked for. Measured what actually happens with two connections: the loser's deferred transaction took a read snapshot before the winner committed, so its first write is refused with `SQLITE_BUSY_SNAPSHOT` (errcode 517, "database is locked"), and the busy handler does not retry that — which is why `busy_timeout` does not cover it. `BEGIN IMMEDIATE` takes the write lock up front, so the loser waits for the winner and then re-applies an idempotent migration. Measured: a connection inside `BEGIN IMMEDIATE` reads the committed `user_version`, a deferred one reads its stale snapshot.
- **The `BEGIN IMMEDIATE` fix is reasoned and probe-measured, not test-pinned.** Reverting it to `BEGIN` survives the suite. The interleaving needs two connections writing concurrently, and forcing it in-process needs a second thread; a barrier between two processes would only detect the bug about half the time, which is a test that passes on broken code. Recorded rather than papered over.
- **Cascades are pinned now, not just foreign-key rejection.** The tests refused a child naming a missing parent but never deleted a parent, while `prune`'s contract is written on top of the cascade — "cascades are not counted: they follow from the schema rather than from the policy". Dropping `ON DELETE CASCADE` from `findings.run_id` or `evidence.finding_id` left the suite green and would have stranded findings and evidence on every retention pass. The existing run-retention test now asserts the finding went with its run and counts the evidence rows left behind.
- **Migration SQL is a `.ts` module, not a `.sql` file.** `tsc` emits only TypeScript, so a `.sql` beside it would exist in the source tree and be missing from `dist` — a daemon that cannot start, discovered at install time. Archestra's `.sql` files are drizzle-kit output: 413 generated files, a `meta/` snapshot directory, a journal, a bundler that copies the folder and two CI scripts that police it. That machinery buys nothing at one migration.
- **Chose `node:sqlite` and recorded it in ADR-0003.** Built into Node, so the daemon adds no dependency and no native build step, and nothing prebuilt enters the supply chain of a tool that reads people's source. Confirmed against the Node in `.node-version`: STRICT tables, `RETURNING` with a subquery, deferrable foreign keys and `user_version` inside a transaction all work, and there is no experimental warning.
- **Rewrote the task's file mode.** It said 0700 for the database file; an executable bit means nothing there. The directory is 0700 and the file is 0600 — and the mode is set _before_ WAL is enabled, because SQLite creates `-wal` and `-shm` with whatever mode the database file carries at the time, and the `-wal` holds rows the database does not yet. Measured: chmod first and all three come out 0600.
- **Upserts on a natural key return the reconciled row.** The stub returned `void`, which cannot work: the stored id is the one that won, so a caller holding its own fresh ULID writes branches that orphan on the foreign key. `shadowPath` is preserved with the id for the same reason — it is derived from the id, so the incoming value names a directory built from a ULID that was discarded.
- **`BranchRef.sessionId` gets no column.** Discovery reports `sessionId: null` for every branch on every sweep, so a stored copy would be cleared seconds after any attribution was written. `agent_sessions.branch_ref_id` owns the link and the branch derives it back — most recently active live session, since a wedged agent that never fired its end hook still holds a row.
- **Two relationships are stored once each, not twice.** `SpeculativeRun.findingIds` comes from `findings.run_id`, and `AnalyzerResult.cached` is answered rather than stored — it says how the caller obtained the result, and a read from the cache table is cached by definition. A run written before its findings existed still reports them.
- **The event log has no foreign keys at all.** It records what happened, so a record must never be rejected because another table lagged; `caused_by` is left dangling past the retention window rather than repaired, since repairing it means rewriting rows in an append-only table.
- **`prune` also covers change sets and cached verdicts,** not just the two the interface doc named. Change sets are the highest-volume entity after events and nothing references them, so a window that skipped them would leave the file growing where it grows fastest — but only _superseded_ ones go: a branch idle longer than the window still has a current change set, and it is the only record of what that branch carries. Runs holding an open or stale finding are kept, because stale means "not re-verified yet" and dropping it retracts a warning rather than resolving one.
- **Replay fixes its upper bound before the first page.** Pagination reads committed rows as it goes, so without it a replay against a live daemon keeps picking up events published while it runs and never terminates. Mutation-pinned.
- **Every upsert got exactly one reader,** so no write is unprovable. That is five additions to the interface and no more; readers the scheduler will want belong to M2.
- **37 mutations, 34 caught** — file and directory modes including the one a hostile umask exposes, the directory `openStore` must not touch, both natural keys, the preserved `firstSeenAt` and `shadowPath`, the dirty null/clean distinction, foreign-key enforcement, the derived session and its ordering, migration rollback, evidence replacement, both cascades, all four prune predicates and the strict `>` that keeps same-millisecond siblings, the replay bound, the cutoff normalisation, the swallowed JSON failure, `makePairKey`'s order independence and all seven enumerated columns. Three survivors: the pinned foreign-key default, which is undetectable by construction, and `BEGIN IMMEDIATE` in each of the two places it is used, which needs two connections writing at once.
- **Corrected an earlier count in this log.** It said 29 of 29 before the enumerated columns landed, which was wrong twice: the distinct set was smaller than 29, and it implied a foreign-key mutation that had not been run — flipping the pragma to `OFF` had been, removing it had not, and removing it is the one that survives.

## 2026-08-27

- **Checked, and the reported parser bug is not one.** A review put ~85% confidence on `--numstat -z` writing a rename as `-\t-\tfrom.bin` in one field, which would make the parser's rename branch dead and report a renamed binary as text. Measured against real git: the fields are `'-\t-\t'`, `'image.bin'`, `'renamed.bin'` — the counts field carries an empty path and the two paths follow, which is what the parser assumes and what its synthetic test encodes.
- **The method behind the finding was right even though the finding was not.** The branch was reachable only through a synthetic input shaped like the assumption; nothing renamed a binary end to end. `commitEveryShape` now does, and inverting the rename branch fails that test as well as the unit one.
- **Added:** a rename that also changes content. Every rename in the fixture was `R100`, so it had no patch section and the alignment for a hunk-bearing rename went unexercised. One line of six changes, which keeps similarity above git's rename threshold — two lines with one edited falls below it and git reports an addition, which is what the first attempt at this fixture did.
- **Added:** `extractChangeSet` run under `diff.renames = copies`. The existing test pinned git's flag semantics on raw bytes; nothing ran the module under the config a watched repository would actually have set.
- **Added:** the marker assertion to the `touchedPaths` revision guard. Its twin asserted the file was never written and this one only asserted the throw.
- **Corrected:** `ChangeSet.snapshotId` was documented "null when clean", which is not what the code means. It records which side was compared, and a dirty branch nobody snapshotted produces a head-only change set that reads identically — the doc claimed a distinction the field cannot carry.

## 2026-08-26

- **Checked the three cross-file items two reviews left open, and all three are already right.** No call site passes a glob or a magic pathspec — the only `*` handling in `core` is Interlock's own scanner, which never reaches git — so `GIT_LITERAL_PATHSPECS` is safe repo-wide. Discovery has no private `required` left; `runRequired` is its only caller and no stderr reaches `remedy` or `details`, so the discipline moved with it. `groupStatus` is rebuilt on `isUnmerged`, and a test asserts a conflicted path counts once as unstaged.
- **Documented:** `GIT_LITERAL_PATHSPECS` is repository-wide policy rather than one caller's accommodation. It applies to every command the runner runs, so a future call site wanting a glob would silently match nothing instead of failing — the one respect in which a `:(literal)` prefix at the call site would have been narrower and safer.
- **Recorded at the stub that owns it:** the object-lifetime problem. A capture writes its tree into the user's object database where nothing references it, so `git gc --prune=now` can collect it between capture and merge — the base-tree fallback covers the next capture, not that. Transferring objects into the shadow promptly closes the window, and a missing object at merge time is retryable rather than an infrastructure failure, which is a constraint on how the store records one. Two reviews reached this independently.
- **Learned:** both reviews' blocking findings were fixed in `repo-handle.ts`, and neither reviewer had that file. A module-scoped review cannot see a runner-wide answer, so a test whose green depends on the runner has to say so — which is why the pathspec and split-index tests now name the argv and the environment that keep them passing.

- **Strengthened again:** the sparse test ran clean, so its tree matched `HEAD` whether or not `add -A` had done anything. It edits inside the cone now, asserts the edit, runs through the intactness wrapper, and covers `index.sparse`.
- **Fixed:** `assertInsideWorktree` accepted `''`, `.` and `sub/..`, all of which git reads as the worktree root — a scoped capture that silently became a whole-tree one while still reporting itself as scoped. Three tests pin it; the guard had none before.
- **Fixed:** `chunkPaths` was exported and unit-tested. Yielding an empty group turns `add -A --` into a whole-worktree stage, and a path over the ceiling has to go alone rather than be dropped. Both were correct and neither was exercised.
- **Fixed:** the linked-worktree test now says which assertion discriminates. The tree matches whichever `HEAD` was read, because `add -A` restages that worktree either way — only `clean` can tell them apart, so it must not be tidied away as redundant.
- **Fixed:** the index helpers take a path, so the linked worktree's own index is measured with the same bytes-and-mtime discipline as the main one rather than a bare buffer comparison.
- **Fixed:** the `core.fsmonitor` fixture used `true`, which starts the builtin daemon asynchronously and drops a socket into `.git` — a race against the listing the test asserts on. A hook path exercises the same config without the daemon.
- **Renamed:** the local `describe` inside `captureDirtyState` to `asSnapshot`. It constructs a value, and `describe` means something else in every file that tests this one.
- **Cut:** three comments that restated a neighbouring one at the same length, including the index-column argument written out twice.

- **Strengthened:** the sparse-checkout test ran against a clean worktree, so its tree matched `HEAD` whether or not `add -A` had done anything under sparsity. It now edits inside the cone, asserts the edit and the out-of-cone file, runs through the intactness wrapper like the rest of the suite, and covers `index.sparse` as well as a plain cone.
- **Measured, and the mechanism I wrote down was wrong.** The comment said `read-tree` marks out-of-cone entries skip-worktree. It does not: a plain `read-tree` into a fresh index sets zero of them — `ls-files -v` reports every entry as `H`. What actually preserves those files is `git add` reading `core.sparseCheckout` and the pattern file itself; with the config turned off against the same index, `add -A` drops the out-of-cone file. So `core.sparseCheckout` must stay readable, and adding it to the runner's scrub list beside `core.splitIndex` would delete every out-of-cone file from every snapshot in a sparse repository. That mutation now fails two tests, which is the guard.
- **Fixed two comments that described closed hazards.** The split-index test said repository config is the layer "the runner's environment scrubbing cannot reach", which stopped being true when the runner started passing `-c core.splitIndex=false`; the pathspec test never said `GIT_LITERAL_PATHSPECS` is what makes it pass. Both now name the runner, so a failure here points at the argv rather than at this file — which is what two reviewers reading this module without `repo-handle.ts` each concluded was a missing defence.
- **Corrected:** the `tempDir` rationale. Leftovers between repositories are not the reason — `mkdtemp` is unique and cleanup is per capture. The reason is that the system temp directory is often a small tmpfs and a large repository's index is not small. Its precondition is stated too: the directory must exist.
- **Wrote down the cost of the vanished-path deferral** on `captureDirtyState`, where a caller reads it: a thrown capture is ordinary and must be non-fatal, and the caller keeps the base tree it had — advancing it on a failure loses the interval, and treating the throw as fatal turns an editor race into a crash. The deferral is only defensible under that contract, and the contract was not written anywhere.

- **Checked, already fixed:** two reviews independently predicted the pathspec-magic and `core.splitIndex` tests were red because the implementation lacked the defence. Both defences exist — `GIT_LITERAL_PATHSPECS=1` in the runner's environment and `-c core.splitIndex=false` in its argv — and both are mutation-pinned. Neither reviewer had `repo-handle.ts`, which is where a runner-wide answer belongs; a review of this module alone cannot see it.
- **Checked, no bug:** sparse checkout does not silently drop files outside the cone. Measured — `outside/b.txt` is absent from disk after `sparse-checkout set` and present in the snapshot, because `read-tree` marks it skip-worktree and `add -A` leaves it alone. Worth a test rather than an argument, as the reviewer said, and it is the repository shape this tool is built for.
- **Fixed a test that could not fail:** the linked-worktree capture asserted only that one path appeared. It now commits divergent content on the linked branch and asserts the tree equals `side^{tree}` and that `clean` is true, which is only possible if HEAD came from that worktree.
- **And reported that fix once before it existed.** The edit did not apply, and the mutation run to confirm it — pointing `rootPath` at the main checkout — is one the _original_ test already failed, because the file it looked for lives only in the linked worktree. A mutation that the old test also catches proves nothing about the new one. The bug the strengthened test actually pins is reading `HEAD` from the main checkout, which the original passed; that mutation is what was needed and is now what runs.
- **Fixed:** the linked-worktree test watched `.git/index` while the index actually at risk was `.git/worktrees/<name>/index`. Both are checked now.
- **Fixed:** the temporary-index cleanup test listed the whole system temp directory, so a capture in another vitest worker could appear as a leak. `SnapshotOptions.tempDir` gives a caller its own root — which a daemon watching several repositories wants anyway — and the test uses a private one.
- **Fixed:** `changedPaths` dropped the source of a rename reported in the worktree column, which is the failure the parser's own comment names. Scoped to that column so an index-column rename source — already absorbed into the base tree and gone from disk — is still excluded. Unreachable in porcelain v1, so it is defence rather than coverage, like the parser branch it completes.
- **Fixed:** `OBJECT_ID` accepted 41 to 63 hex characters, which no git produces and the error message did not claim. SHA-1 or SHA-256 length now.
- **Fixed:** the chunking test hardcoded the ceiling it was testing; the constant is exported and read.
- **Corrected:** the doc comment said `repo.gitDir` is "one of the places the temporary index must not land", which is checked in the runner rather than here, and said nothing about submodules while claiming uncommitted work is included. A gitlink records the submodule's HEAD, so work inside one is not captured.
- **Deferred, with reasons:** a whole-tree fallback when the vanished-path race fails an `add` — the watcher's next event captures it, and a retry path costs more than the lost snapshot; `GIT_OBJECT_DIRECTORY` pointing captures at the shadow's object store — a real design question, but it belongs with `commitSnapshotInShadow`, whose stub is where the object-lifetime problem lives; `WorktreeSnapshot.baseTreeOid` for a self-describing chain, until something holds a chain.

## 2026-08-25

- **Fixed:** `overlappingPaths` compared destinations only, so a rename on one branch and an edit to the same file on the other reported no overlap — a rename/modify conflict git cannot resolve, invisible to the cheapest signal there is, and a pair that never appears is never compared. A rename now counts under both of its names, which is what `touchedPaths` already did: two functions answering the same question have to agree or one prioritises a pair the other drops. It had no tests at all; it has four now, and reverting the fix fails two.
- **Fixed:** `assertInsideWorktree` let an empty path through. Neither absolute nor climbing out, so it reached git, which ends the capture with `empty string is not a valid pathspec` — an error about pathspecs where the function exists to produce one about the caller. The two shapes it did catch were tested; the third was the one nobody thought to report.
- **Fixed:** `mergeBase` took its revisions unguarded, the third instance of a class already fixed in `worktree.ts` and `diff.ts` and the only exported git call left without it. Measured: `--is-ancestor` in the first slot turns `merge-base` into an exit-code test that prints nothing, which this reads back as "no common ancestor" — a pair dropped from analysis in silence. `assertObjectId` is the wrong guard here because a ref name is not an object id, so `assertRevision` sits beside it and refuses only what git parses as an option. `--` is added too, though it is hygiene: it separates revisions from paths, and a flag-shaped value sits before it.
- **Found, not changed:** a whole-tree capture of a worktree holding an untracked nested repository writes a tree with a gitlink to a commit that is not in the outer object database — reproduced, `cat-file` cannot resolve it. `git add -A` does this and warns, so the snapshot is recording what git would record and changing it here would make the tree differ from the worktree. It surfaces later, where a merged tree is checked out into a shadow worktree, and belongs to that task rather than this one.

- **Fixed:** `diff.test.ts` carried eighty lines twice over and the stale rename-config test alongside its replacement — five tests defined in duplicate, one of which could not fail. Removed: 42 tests to 34, and the suite lost the redundant fixtures.
- **Decided:** `resolveTarget` is internal again. It was exported for a unit test that never arrived, and the coverage it was missing was `touchedPaths` — the entry point whose guard nothing exercised. Testing the call site is worth more than exporting to reach the helper, and dropping the guard now fails four tests instead of two.

- **Fixed:** `diff.interHunkContext` merges hunks that sit near each other, so a repository setting it to 10 reported two separated edits as one region — measured, and `--inter-hunk-context=0` restores the split. `--unified=0` asks for it and does not guarantee it. Same family as `diff.renames`, now pinned beside it. The existing config test could not have caught this: its oracle shells out to raw git without the pinning flags, so repository config moves both sides of the comparison together. A fixed expected count is what catches it.
- **Fixed:** only two of the three diff invocations carried `--`. They exist to produce the same file set in the same order, and `alignHunks` now throws when they disagree, so divergent argv between them is the likeliest way to make that throw fire for no reason.
- **Clarified:** `--` was never what closed the flag hole. It separates revisions from paths, not from flags; `assertObjectId` does that work. The separator is hygiene.
- **Fixed:** the revision guard was four lines duplicated between `extractChangeSet` and `touchedPaths`, and only the first copy had a test. Collapsed into `resolveTarget`, which is the same argument that produced `DIFF_BASE`, `runRequired` and `parseStatus` — one place to get right, one place to test.
- **Fixed a test that passed through the wrong assertion:** the typechange fixture gave every file one hunk, so the per-file checks read 1 under the bug as well and only the total caught it. `zzz.txt` now has two separated edits, so a file inheriting the typechange's second section reads one hunk instead of two.
- **Fixed:** `alignHunks` raised with `infra: false`. A git whose output shape does not line up is the toolchain, which is what the flag is for — never a property of the code under analysis.
- **Fixed:** `assertObjectId` put the offending value in `details`, which is documented as carrying neither secrets nor file contents. It arrives from a caller, may be anything, and the error reaches the API and the agents; the field name says enough. Carried over rather than introduced, but it runs on three call sites now.
- **Fixed:** hardcoded `/tmp` paths in the tests, which the style rules name explicitly, and the flag-shaped-revision test asserted only the throw rather than the file not being written — which is the harm it is named for.

- **Fixed:** a typechange broke the positional hunk-to-file matching. git reports a file becoming a symlink once in `--name-status` and `--numstat` but **twice** in the patch, as a deletion and a creation — measured, in both directions — so every file after one took the wrong section. The mirror of the case `parseHunks` already guarded against, reached by an extra section rather than a missing one. Sections are now counted per entry, and the alignment moved into an exported function so the guard beneath it could be tested at all.
- **Added:** the alignment raises when the summary and the patch disagree on how many files changed, instead of absorbing it. `hunks[index] ?? []` made a length mismatch silent, and a hunk attributed to the wrong file is evidence pointing at the wrong branch — worse than none, because it reads as certainty. Unreachable through git now that typechanges are handled, so it is unit-tested directly; the integration suite passed with the check removed.
- **Fixed:** `parseBinaryPaths` split a `--numstat -z` field on every tab and took the third piece, so a path containing a tab was truncated — `has\ttab.bin` became `has`. The real file lost its binary flag and a phantom entered the set. `-z` quotes nothing, so the first two tabs are the separators and everything after is the path. The suite covered space, newline and quote; tab is the one that is this format's own delimiter.
- **Fixed:** revisions reached git unvalidated. Verified that a `snapshot.treeOid` of `--output=<path>` made git **write a file outside the repository** — `--` separates revisions from paths, not from flags, so a flag-shaped revision is acted on. `assertObjectId` existed one file away in the same branch and had not been applied here; it now lives beside the runner and both callers use it, with `--` closing the argument list as well.
- **Fixed a test that could not fail:** the copies half of the rename-config test compared two identical lists. `commitEveryShape` had no copyable file, and even with one, a copy normalises to an addition with no previous path — byte-identical to what the flag produces — so a change set comparison cannot tell them apart however the flag behaves. It provokes a real copy now and asserts on git's own `--name-status` bytes with and without the flag. **Written twice before it was true.** The first attempt did not apply at all and the log claimed it anyway. The second sliced the file with the two anchors in the wrong order, which duplicated eighty lines and left the old test in place beside its replacement — and the change-detection check passed, because the file did change, just not into what was intended. A test suite carrying both the old and the new version of one test says nothing about which is authoritative. Verify the edit by reading the result, not by whether it reported success.
- **Fixed:** two spliced lines in the dirty-state task, one of which read as a false start. `proseWrap: preserve` means the formatter does not catch an over-long line, so a hand edit has to.

- **Fixed, hard rule 1:** a repository with `core.splitIndex = true` had every capture leave a `sharedindex.*` file in the user's `.git`. Reproduced — one file after a whole-tree capture. Repository config is the layer the runner's environment scrubbing cannot reach, `GIT_INDEX_FILE` does not move that write, and the users most likely to have it on are the ones with the large repositories this is built for. The runner now passes `-c core.splitIndex=false` beside `core.fsmonitor`. `core.fsmonitor = true` was already covered; `core.untrackedCache` only touches the temporary index. The test suite missed it because no fixture set the config.
- **Fixed:** the scoped filter read both status columns. The index column compares the seeded index against `HEAD`, so a change already absorbed into the base tree reports forever — a file deleted in batch one reports `D` in batch two, and restaging it finds nothing on disk and nothing in the index, which `git add` treats as fatal. Reproduced: batch two threw. Only the worktree column now, which is the question the filter was documented as asking. A rename's source needs no special handling for the same reason — git reports it as a deletion in that column and its destination as untracked, so both arrive on their own account, and `origPath` is no longer consulted here.
- **Fixed:** reported paths reached git as pathspecs with magic enabled. Verified `:(exclude)tracked.txt` silently dropped a file from a capture and reported success, and a file genuinely named `:(top)x` could never be captured. `GIT_LITERAL_PATHSPECS=1` in the runner's environment closes it for every call site rather than for this one.
- **Added:** a linked-worktree capture test. `worktreePath` and `repo` are separate arguments precisely because they differ there, and the plan calls it the ordinary case, but every fixture used a single directory.
- **Added:** direct unit tests for the status parser. Its callers reach it through a real repository, which cannot produce an unmerged entry without a conflicting merge or a newline in a path on every filesystem.
- **Learned again:** the assertion helper must be ordered against the tool it measures. `observable()` runs `git status`, and with `core.splitIndex` set the _user's_ git writes `sharedindex.*` during that call — which the new test blamed on the capture until the directory listing moved inside the wrapper, after the observation, where the index bytes and mtime already were.
- **Not changed:** `runRequired` still marks a non-zero exit `infra: true`. Exit 128 means bad arguments and a corrupt repository alike, and keying on it would mislabel the second to fix the first. The caller bugs that reached it are now refused by name before git sees them.
- **Out of scope:** the five `it.todo`s in `user-repo-untouched.test.ts` are their own M1 task with its own checkbox, not this one.
- **Done:** M1 ChangeSet extraction. `extractChangeSet` and `touchedPaths` in `diff.ts`, asserted against `git diff` for the same range rather than against literals — a literal records what the implementation did the day it was written, and this has to keep agreeing with git.
- **Found:** rename detection is configurable per repository, and it changes the _shape_ of the output, not just its quality. Measured: with `diff.renames = copies` in `.git/config`, `--name-status` emits `C100|copysrc|copydest` where an addition belongs; with `false` it stops pairing renames at all. Passing `--find-renames` explicitly overrides both. A watched repository must not decide what Interlock records, and this is the same class as `core.fsmonitor` — repository config reaching into results the runner's environment scrubbing cannot.
- **Found:** `--name-status -z` reports a rename **source first, then destination**. `status --porcelain -z` reports the destination first. Two parsers in the same package, opposite orders; noted in both.
- **Decided:** hunks are matched to files by position, not by name. git emits one `diff --git` section per changed file in the same order as `--name-status` — verified with seven files including a binary, a mode-only change and a rename — and reading the path back out of that line means undoing git's quoting for names holding a space, a quote or a newline. A file with no hunks still gets a section, so dropping empty ones would shift every later file's hunks onto the wrong path.
- **Decided:** a copy is recorded as an addition with no `previousPath`. `previousPath` means the path no longer exists, which a copy's source still does. Unreachable through git with `--find-renames`, so it is unit-tested on the exported parser — the integration suite passed with the branch inverted.
- **Found while testing:** a pathspec narrows rename detection when _asking git for the answer_ too. Scoped to a rename's destination, `git diff --unified=0` counts one hunk for content that never changed, which failed the comparison until the helper named both paths. Same trap as the snapshot filter, one layer out.
- **Decided:** an unrecognised status letter counts as modified rather than being dropped. It still names a path that differs, and understating what a branch touches is the error this cannot afford — a pair that never appears is never compared.
- **Removed:** `DiffOptions.pathsOnly` and `DiffOptions.ignore`. `touchedPaths` already is the cheap path, and two ways to ask for one thing is the second spelling of nothing the config parser dropped. `ignore` went with it because the watcher owns applying `config.ignore`, and it was declared here and read nowhere.

- **Fixed a test that passed for the wrong reason.** `splits a path list too long for one command line` built 4,000 paths of 15 bytes — 60,000 against a ceiling of 98,304, so it never split, and the chunk branch showed uncovered. It now uses long names to cross the budget with fewer files, asserts the budget is over the ceiling before running, and counts `add` invocations through a recording runner, so the claim in the name is the thing asserted. Pinning the ceiling at `MAX_SAFE_INTEGER` fails it.
- **Fixed:** `assertObjectId` shipped with no test — the mutation that deleted it passed the whole suite, and coverage flagged the throw. Four shapes now cover it, including a flag-shaped value and a revision expression, which are the two that would reach `rev-parse` as something other than an id.
- **Widened:** the scoped-equals-whole-tree property from two undo scenarios to six, covering a further edit, a deletion, a new untracked file and a mode change. It is the strongest invariant this function has and the one that would have caught the filter bug, so it is worth a table rather than a spot check.
- **Added:** failure injection on the scoped route. `failingAt('write-tree')` only exercised the whole-tree path, and the scoped path runs read-tree, status, add and write-tree — a failure inside `add` leaves a partly populated temporary index, which is the case the shared cleanup was untested against.
- **Coverage:** `worktree.ts` 89.55% → 95.52% statements, 91.80% → 98.36% lines. The one line left is `commitSnapshotInShadow`'s `notImplemented`, which belongs to a later milestone.

- **Fixed, and it was the same bug one step later:** the scoped filter asked `git status` against the _user's_ index, which answers "is this dirty relative to HEAD". A file the user reverted answers no, so the capture returned the base tree still holding the edit — reproduced: base holds `EDITED`, worktree holds `committed`, status reports nothing, scoped capture returns `EDITED` while a whole-tree capture returns `committed`. Wrong, not stale, which is exactly what requiring the base tree in the scope type was meant to make unrepresentable. `git checkout -- file`, a stash, an editor undo and an agent backing out its own edit all produce it. The index is now seeded before the filter runs and the filter asks against that index, which is the question the scoped path actually has.
- **Fixed:** `clean` inherited the same error, reporting `clean: false` for a worktree that was clean. It follows from the tree being right.
- **Fixed:** the runner validated `indexFile` only for the index-writing class, so a read-only verb carrying a redirection skipped the check. Nothing wrote through it, but the guarantee is that the runner validates the redirection — and the fix above puts `status` under `indexFile`, which is precisely that combination. Validated whenever present now, and pinned: guarding only the writing class fails a test.
- **Decided:** snapshot paths are worktree-relative, and one that escapes is refused by name. Verified that `../escape.txt` and an absolute path previously took the whole capture down with an infra error about git. Filtering them silently would leave the snapshot quietly missing whatever the caller meant; a typed refusal names the caller's bug while the watcher is still being written against the contract.
- **Added:** scoped and whole-tree captures of the same worktree must produce the same tree. The Done-when only asked that the user's repository be untouched, which says nothing about whether the tree is right — the half findings 1 and 2 fell through.
- **Noted:** the worktree-column rename branch in the status parser is unverifiable against current git, which reports a worktree rename as a deletion plus an untracked file with `status.renames` set or not. Recorded as defence rather than coverage, so nobody later reads it as tested.
- **Hardened:** `baseTreeOid` is checked against an object-id shape before being interpolated into a `rev-parse` argument; the temporary-directory cleanup no longer replaces a diagnostic with its own failure; `MAX_PATHSPEC_BYTES` says it counts the paths alone and that the runner's arguments and the environment share the same budget.

- **Done:** M1 dirty-state snapshots. `captureDirtyState` builds a tree from a temporary index outside the repository. Measured in isolation: `.git/index` comes back byte-identical _and_ mtime-identical, so the capture is invisible to the user's own git rather than merely non-destructive.
- **Rewrote the task, because it specified a corrupt snapshot.** It said to seed a scoped capture with `read-tree HEAD`. Reproduced: with `a.txt` edited in an earlier round and only `b.txt` reported this round, the tree records `a.txt` at its committed contents while the worktree holds something else — silently wrong, the same class of failure the task warns about for the unseeded case. The seed has to be the previous snapshot's tree, and a scoped scope now carries it in the same object so the broken form cannot be expressed.
- **Decided:** a scoped capture that loses its base tree falls back to a whole-tree one. That is where the task's "treat a missing-object error on a stored tree OID as re-snapshot" note lands, and the check is `rev-parse --verify` rather than matching stderr — git's wording for a missing object has changed between versions and the `-h` parsing already taught that lesson once.
- **Found:** `git add` refuses a path it is told to add that is ignored (exit 1) and fails outright on one matching nothing (exit 128) — a file created and deleted inside a debounce window. Both are ordinary watcher output. `--ignore-errors` does not help, and `check-ignore -z` needs `--stdin`, which the runner closes. Filtering the reported paths through `status --porcelain -z` solves both, and reuses the parser discovery already needs.
- **Found:** a pathspec narrows git's rename detection too. Scoped to a rename's destination alone, status reports `A renamed.txt` with no source, so the source is never restaged and the tree keeps a file that is gone. Recorded on `SnapshotScope.paths`: both halves have to be reported, which a watcher sees because both moved.
- **Found:** `git status` rewrites `.git/index` to refresh its stat cache — contents byte-identical, mtime moved. An assertion that reads status and the index together fails on its own observation. Measured with `fs` before any git command, both hold; the task's note about mtime flakiness now says which order avoids it.
- **Extracted:** the NUL status parser and `runRequired` into `status.ts` and the runner, rather than copying either into `worktree.ts`. Discovery's 38 tests passing unchanged is what says the refactor was behaviour-neutral.
- **Decided:** pathspecs are split across invocations against the same temporary index. Arguments and environment share a fixed per-process budget and the runner closes stdin, so a watcher reporting thousands of paths cannot be piped and must not be truncated.

## 2026-08-24

- **Fixed, hard rule 4:** `parseRepoConfigOverride` echoed unknown key names into the error. A key is chosen by whoever writes the repository exactly as a value is, so `{"IGNORE ALL PREVIOUS INSTRUCTIONS...": 1}` travelled verbatim into a `CONFIG_INVALID` that reaches the API and the agents. Measured: 4,000 unknown keys in a 38 KB file produced a 78 KB message, and one 60 KB key produced a 60 KB one — the file cap bounded the file and nothing bounded the error. Unknown keys are now counted, not named, and the accepted set is what the message carries; the same inputs produce ~100 bytes. Truncating was rejected: it bounds the size and not the injection, and this is the same call the runner already makes about git's stderr.
- **Found the invariant was written down, tested, and contradicted in the same function.** The docstring said "never the value found" and a test asserted a secret in an `ignoreBranches` entry does not leak — both true, both about values. Two tests above, `refuses a key it does not recognise` pinned the echo with a benign key name, so nothing ever failed. The value test is now a table covering a value, a top-level key and a key inside `toolchain`, and a second test asserts the complaint stays smaller than the file that caused it.

- **Answered:** nothing re-reads `.interlock.json`. `describeRepo` is `readRepoConfig`'s only caller, and its own docstring says it describes a first sighting — so the stored `Repo.config` is the last word and a repository editing its overrides mid-session is never honoured. The two failure modes point the wrong way: refusing a malformed file is loud and reachable once, serving a stale one is silent and permanent. Not built here — the watcher is what notices the file changing and the store is what holds the copy — so the requirement is written into both tasks, including that a refusal on re-read keeps the last good config rather than dropping the repository from the watch set.
- **Recorded:** `describeRepo` throwing for one repository's broken file must not stop a sweep. Same shape as the unreachable worktree, one level up, and the watcher is where it gets contained.
- **Fixed:** `validateConfig` checked ranges without checking types, and the config arrives through `JSON.parse` where every field is `unknown`. Measured: `debounceMs: "abc"` passed validation and made `Date.now() + debounceMs` the string `"1787595958871abc"` — a deadline that can never be met, silently. `repos: "dist"` iterated characters and produced one absurd problem per letter. Types are checked first now, and `logLevel` against its union.
- **Fixed:** `O_NOFOLLOW` and `O_NONBLOCK` are `undefined` on a platform without them, and `O_RDONLY | undefined` degrades to a plain read that follows symlinks and blocks on fifos. A security flag that disappears silently is worse than one that is absent, so their absence is refused at load.
- **Fixed:** `EACCES`, `EMFILE` and `ENOSPC` all read as "could not be opened". The errno is kept in `details`, and a read that fails midway is shaped like every other failure in the module rather than escaping as a raw fs error.
- **Fixed:** a leading byte-order mark made a valid file parse as "not valid JSON". Stripped.
- **Decided:** an absent key and one holding nothing collapse to the absent spelling — `toolchain: {}` and `ignore: []` both disappear. One stance, applied in both places rather than one.
- **Noted:** `*` in the glob crosses `/`, so `src/*` matches `src/deep/file.ts`. Right for branch names, wrong for the intuition `.gitignore` teaches, and `config.ignore` is a path list. Recorded against the watcher, which is what will consume it.

- **Fixed, and the comment claiming it was handled was wrong:** `readRepoConfig` opened the file before checking what it was, so a `.interlock.json` created with `mkfifo` blocked at `open` waiting for a writer and never reached the `isFile` guard the comment credited. Measured: the plain open never returned and held the process; with `O_NONBLOCK` it returns at once and `isFile` refuses it. Removing the flag now fails the test after 30 s, which is the shape of the bug.
- **Fixed:** the file was followed through a symlink. Pointed at a docker config outside the repository, the parse refused it — and named `auths` and `credsStore` in the error, so the key structure of an arbitrary readable file came back one file per attempt. Values never leaked, which was the rule that held; key names were not covered by it. `O_NOFOLLOW` refuses the link instead. The skill already carried this lesson for `indexFile` and it was not applied here.
- **Fixed:** the size ceiling gated on `st_size` and then read to EOF regardless, so any file whose reported size understates its readable length walked past both checks — every procfs file reports zero, and CI runs ubuntu. The read is now bounded to the ceiling plus one byte, in a loop because a single `read` may return short.
- **Fixed:** an unreadable override file was reported `infra: true`. Infra is for a broken environment; a file inside a watched repository is repository state.
- **Fixed:** `{"toolchain": {}}` round-tripped to `toolchain: {}`, giving an absent key and an empty object two shapes for the same meaning. The key is dropped when no command survives, and the object is built field by field so the compiler enforces the key set the loop was enforcing by hand.
- **Noted:** `JSON.parse` keeps the last of a repeated key, so a file declaring `ignore` twice parses as valid. Recorded in the doc comment rather than fixed — "every problem at once" sets an expectation this one case cannot meet without a streaming parser.

- **Done:** M1 repo config override. `describeRepo` reads `.interlock.json` into `Repo.config`; `parseRepoConfigOverride` validates it in `shared`, reporting every problem at once the way the global config does.
- **Decided:** a malformed override refuses the repository rather than falling back to the defaults. The file decides which branches go unexamined, so ignoring it would have Interlock watch work the repository asked it to leave alone and say nothing about why — the failure shape this project rejects everywhere else.
- **Decided:** an unrecognised key is a problem, not something to skip. A typo that quietly does nothing is indistinguishable from a setting that was never applied.
- **Decided:** problems name the key and the expected shape, never the value found. The file is repository content, it may hold a secret, and an `InterlockError` travels to the API and to agents. A test asserts a token pasted into the file does not appear in the complaint.
- **Decided:** the file is opened once and measured through the handle, so the file that is sized is the file that is read. `isFile()` does what a size check cannot — measured `/dev/zero` at size 0 with a read that had not returned after three seconds, and a directory at size 288.
- **Noted:** reading the file is not applying it. `listBranchRefs` still takes its own `ignoreBranches` option and nothing passes `Repo.config.ignoreBranches` to it, so ignore rules are stored and not yet honoured. Recorded against the watcher task, which is the caller that wires them.
- **Noted for M3:** `toolchain` commands come from repository content. Validation here proves a non-empty string and nothing more; the sandbox is what makes running one acceptable.

- **Fixed:** the `audit` job installed dependencies it never read. `pnpm audit` resolves advisories from `pnpm-lock.yaml` alone — verified by running it in a directory holding only the lockfile and manifest — so the install was the slowest step of the cheapest job.
- **Fixed:** `bench` ran weekly only, which leaves a regression unattributed for up to seven days. It now also runs on pushes to `dev`. Not `main`, as suggested: `main` is release-only here, so a push trigger there would find the regression at release time instead of at the commit that caused it. Inert until benchmarks exist — `pnpm bench` prints budgets and writes nothing.
- **Not applied:** flipping `if-no-files-found` to `error`. Right once a report exists, but `pnpm bench` writes no files, so it would fail every run and turn a scheduled job permanently red. Recorded against the M8 task that implements the benchmarks, together with printing last-versus-current p50 and p95 so the job reads itself.
- **Documented:** why the macOS cell is load-bearing. It carries three things, none obvious: `/var` resolving to `/private/var`, which discovery depends on; FSEvents for the watcher; and Apple's git, several minor versions behind Ubuntu's, which is the only cross-version coverage the flag-allowlist `-h` parsing gets. Both Linux cells are `ubuntu-latest`, so that axis is Node alone.
- **Fixed:** `cancel-in-progress` applied to pushes as well as pull requests, so a second push to a shared branch would cancel a run mid-test and leave the first commit unverified. Keyed on the event so only pull requests cancel.
- **Noted:** `pnpm audit` currently reports one high advisory — `nanoid <3.3.18` reached through `vitest → vite → postcss`. Dev-only and not shipped, which is exactly the case the job is advisory for; the new annotation will surface it on the next run rather than leaving it in a log nobody opens.

- **Fixed, same day the rule was written:** the branch added "CI runs the same scripts `pnpm verify` runs" to CLAUDE.md and then shipped a gate that only CI ran. Coverage thresholds apply under `--coverage`, and `verify` ended at `pnpm test`, so a floor could go red in CI after a green local run — exactly the drift the rule forbids. `verify` now ends at `test:coverage`, confirmed by raising a floor and watching `pnpm verify` exit 1.
- **Fixed:** coverage counted `packages/dashboard`, which the workspace excludes by name and CI never installs, builds or tests. A package the pipeline does not touch was in the denominator at 0%, so every file added to it would have walked the floor down and tripped it on unrelated work. Excluding it: statements 82.14 → 84.49, functions 61.98 → 65.78, lines 84.34 → 87.18.
- **Removed the `functions` floor.** Nineteen of the thirty-nine uncovered functions are `notImplemented` stubs, and CLAUDE.md requires that pattern — so the number tracks how much of the plan is unbuilt rather than how well the built code is tested, and following the mandated rule would have walked it into the floor within a milestone. Statements, branches and lines move with real code and are set just under actual (82/82/85); a stub file contributes one statement, an untested real module contributes many.
- **Fixed:** coverage was enforced on all three matrix cells against global floors. One cell carries it now; the others run the suite plain. Identical floors on three platforms buy nothing, and the first platform-conditional branch would have produced a floor that passes on ubuntu and fails on macOS.
- **Fixed:** the `audit` job was `continue-on-error` on its only step, so it was green unconditionally and its findings lived in a log nobody opens. A failed audit now emits a `::warning::` annotation on the run summary. Written with two trivial `run:` lines rather than a shell block that captures and formats output — the reason recorded for skipping `actionlint` is that no workflow here contains real shell, and that reason has to stay true or the decision has to change.
- **Fixed:** the static job was named for three of the four things it runs, so a build failure appeared under "lint · format · typecheck".
- **Fixed:** `bench` used `cancel-in-progress` with a rationale about superseded runs, but both its triggers resolve to the default branch — a manual dispatch would have killed the weekly run. Serialised instead.
- **Documented:** `cla.yml`'s permission block is the set the action's own README requires, `actions: write` included, for re-running the check when a signature arrives after the pull request opened. Left as-is rather than trimmed on a guess: it is load-bearing for the licence model and cannot be tested without a fork pull request.

- **Corrected a premise:** the repository already had formatting and coding-style CI — Prettier and type-aware ESLint, on every matrix cell. What it lacked was hardening and shape.
- **Fixed:** every action was referenced by a mutable tag. The one that mattered is `cla.yml`, a third-party action running under `pull_request_target` with write permissions and repository secrets; moving that tag would have run arbitrary code with the repository's token. All five are pinned by commit SHA with the version in a comment, which Dependabot still updates.
- **Fixed:** workflows ran with the default writable token. `ci.yml` and `bench.yml` now declare `contents: read`, and every job has a `timeout-minutes` — this suite spawns processes and deliberately kills some, and a wedged one would otherwise hold a runner for six hours. A regression earlier in this session hung the test process for ten minutes, which is what that would have looked like unbounded.
- **Restructured CI by what actually varies.** Lint, formatting and types are properties of the source, so they run once instead of four times. Tests keep a matrix, but macOS earns one cell rather than an axis: it is where `/var` resolves to `/private/var` and where the watcher will use FSEvents, and its minutes bill at ten times Linux. Four cells became three plus one, and three of the four ESLint runs are gone.
- **Decided:** CI invokes the same scripts as `pnpm verify`, split into `verify:static` and the test run, rather than a hand-copied list of the same commands. A workflow that reimplements the local gate is how the two end up green while checking different things.
- **Folded** the coverage job into the test job. A separate job paid a second checkout and install to run the same tests again; `test:coverage` on every cell costs nothing measurable on a six-second suite.
- **Added** `git --version` to the test job. The flag allowlist is asserted against whatever git the runner ships, and a rendering change in git 2.55 already broke CI once.
- **Rejected:** `actionlint` and `zizmor` for now. Every `run:` in these workflows is a single `pnpm` invocation, so there is no shell for actionlint to check, and adding third-party actions to lint three files cuts against the pinning just done. Revisit when a workflow grows real shell.
- **Rejected:** `knip` for now. Half the packages are declared surface that throws `notImplemented`, so it would flag the planned API as dead. It becomes useful once M1 fills those in.
- **Rejected:** a nightly run against the latest git. Every pull request already runs against the runner image's git, so drift surfaces within a day of an image update, and the allowlist test now parses flag tokens rather than matching prose.
- **Checked, no change:** fixtures already set `user.name` and `user.email` locally, so they do not depend on a global git identity. `pnpm clean` already removes `.tsbuildinfo` alongside `dist` — deleting `dist` while leaving the build info makes `tsc --build` emit nothing and type-aware lint fail with 55 unresolved-type errors, which is worth recognising rather than fixing.

## 2026-08-22

- **Fixed:** worktree filtering depended on a porcelain detail. On git 2.39 a locked worktree whose directory is gone prints `locked` and not `prunable`, which is what the code assumed — but whether `worktree list` annotates both has moved between versions, and CI runs 2.55. The filter now skips a worktree that is prunable **and not locked**, which is correct under either behaviour and matches what `worktree prune` actually does. The asymmetry is deliberate: an unlocked missing worktree is garbage awaiting pruning and holds no observable work, a locked one is state someone chose to keep and merely cannot be reached, so it is unknown.
- **Fixed:** `matchesGlob` tested a literal match before `*`, so a name containing a literal `*` could consume the wildcard meant to span it — `*?` against `*` answered false. Refnames forbid `*` and `?` so it was unreachable, but the matcher is now correct standalone. 28 cases pass, including the greedy-overshoot and give-back cases a scanning matcher usually fails.
- **Deferred with a reason:** a per-`status` timeout shorter than the runner's default. Discovery reads worktrees serially, so an unreachable-but-not-absent worktree burns the full timeout before returning unknown. Picking a bound without the watcher's latency numbers would trade that for reporting a slow but working worktree as unreadable, which is the worse error. Recorded in the watcher task, where the numbers get measured.
- **Removed:** the `init -b main` unborn-HEAD test. An unborn HEAD resolves symbolically, so that fixture cannot tell correct behaviour from a hardcoded `main`; the `trunk` variant beside it can.

- **Reviewed the rest of M1 against what the runner and discovery now do.** Six tasks moved. Dirty-state snapshots: the recipe was checked end to end through the hardened runner first — seed, scoped `add`, `write-tree`, ignored file excluded, user index byte-identical — so the task stands, with the `indexFile` rules the guard now enforces written into it. SQLite store and `interlock status`: `dirty` is three-valued now, and a schema or a display that folds unknown into clean loses the distinction for good. Watcher: a worktree with no dirty state has no OID to deduplicate against. ChangeSet extraction: `--numstat` and `--name-status` are computed internally and safe, but a patch-producing diff runs the repository's `diff.external` and `textconv` drivers, so those need `--no-ext-diff --no-textconv` — measured both ways. User-repo-untouched: hash the git dir a linked worktree shares, and make the cycle include a snapshot so the index-only path is covered rather than skipped.
- **Unchanged:** repo config override, daemon skeleton, agent session hooks, and the single-git-call-site test. Nothing this session touched what they depend on.

- **Fixed, and the previous fix was wrong:** collapsing runs of `*` stopped `****x` from backtracking and did nothing for the pattern class that matters. `a*a*a…b` against a name of `a`s is exponential in the number of wildcards, not their adjacency: 20 seconds at a 33-character pattern, and the 200-character cap allows 99 of them. The test shipped last round aimed at the adjacent case, so a live denial of service sat behind a green assertion — the worst state a security control can be in. `matchesGlob` no longer builds a regex at all; it scans with a single backtrack point, bounded by the product of the two lengths. The reviewer's 198-character pattern now takes 0 ms, and the test covers both classes.
- **Decided:** `BranchRef.dirty` is nullable, and `null` means the worktree could not be read. A worktree marked with `git worktree lock` — which is what a worktree on a removable volume is locked for — keeps its listing when its directory disappears, so it carries `locked` rather than `prunable` and reaches `status`, which exits 128. Failing there took down discovery for every branch in the repository, and reporting it clean would have been worse. Making the field nullable put the choice in the type: `contentIdentity` now returns `null` for an unobserved worktree rather than the head alone, which would have let a result computed from a clean tree be reused for a tree nobody read.
- **Fixed:** `parseStatus` consumed the source field of a rename, and nothing tested it. Removing that line still passed all 22 tests while producing the garbage path `xt` in both file lists, from `a.txt` read as a status entry.
- **Fixed:** `discovery` had its own weaker `verbOf` beside the runner's correct `subcommandOf`, which skips global-flag values. Not reachable — every call site here leads with the verb, and the runner refuses reserved global flags — but two implementations of one idea in one package is how the wrong one gets copied. `subcommandOf` is exported and shared.
- **Corrected:** the default-branch comment claimed `main` was the fallback for an unborn HEAD. An unborn HEAD still resolves symbolically, so a repository initialised on `trunk` reports `trunk`; the literal `main` is reached only by a detached HEAD with no `origin`. Both paths are now tested, and the `origin`-only limitation is stated where it is decided.
- **Removed:** `WorktreeEntry.head`, parsed and stored and never read — `headSha` comes from `for-each-ref`.

- **Fixed:** `matchesGlob` translated every `*` to its own `.*`, so adjacent wildcards backtracked exponentially — measured 157 ms at ten stars, 25.5 s at sixteen, against a sixteen-character branch name. Ignore patterns come from the repository's own config, which the watched agents write, so a checked-in file could stall the discovery loop for as long as its author liked. Runs of `*` now collapse to one wildcard; the same pattern takes 1 ms.
- **Fixed:** `?` was missing from the escape set and survived into the regex as a quantifier, so `wip?` kept `wipe` and dropped `wip` — the exact inverse of the glob. It now means one character.
- **Fixed:** `mergeBase` returned `null` for any non-zero exit. git exits 1 for refs with no common ancestor and 128 for a ref it cannot resolve, so a typo or a stale ref reported as "not comparable" and the pair was dropped from analysis in silence. Only exit 1 is an answer now.
- **Decided:** a repository has one identity, and it is the main worktree's path. `--show-toplevel` answers with the worktree the path sits in, so opening the same repository through a linked worktree produced a second `Repo` row and a second shadow clone — on M1's main path, whose exit criterion is three worktrees under active edit. git lists the main worktree first from anywhere in the repository, which is the one path every worktree agrees on. The alternative, keying identity on `--git-common-dir` and carrying the worktree separately, would change the handle type and the store schema for no gain here.
- **Fixed:** `required()` put raw git stderr in `remedy`. stderr names branches and paths, `details` is documented as carrying neither secrets nor file contents, and an `InterlockError` reaches the API and the agents — so the diagnostic is gone from the error and the remedy says what to do. The runner already logs the failing command through the redacting sink.
- **Fixed:** `dataDir` was optional, so `describeRepo` without one produced a shadow path at the filesystem root. It is required by the type that needs it rather than checked at runtime.
- **Fixed:** an unmerged path has both status columns non-blank, so it was reported as staged and unstaged at once. It counts once, as unstaged.
- **Fixed:** `readDirtyState` ignored the exit code, so a failed `status` read as a clean worktree. Unreachable through `listBranchRefs` today — git marks an unreadable worktree prunable and it is dropped first, and an unreadable subdirectory only produces a warning with exit 0 — so the fix is unpinned by a test. Kept anyway: "clean" is the most dangerous wrong answer this function can give.
- **Rejected:** hardcoding `main` as the last-resort default branch and `origin` as the remote consulted. Both are documented fallbacks for a repository that declares nothing, not guesses that override what a repository does declare. Multiple remotes are a real limitation, and the day one matters is the day the config override lands.

## 2026-08-21

- **Added:** a test pinning that a SIGTERM the runner did not send is reported as a signal death, not a timeout — a fake git that runs `kill -TERM $$`. Reviewed three times as a live bug and disproved twice by measurement; it is now a test, so it stops being an argument. Conflating the two branches fails it.
- **Decided:** the test suite is POSIX-only, deliberately. Spy scripts are `#!/bin/sh`, modes come from `chmod`, and `HOME` is stubbed where Windows would need `USERPROFILE`; CI is ubuntu and macOS. `windowsHide` in the runner is cheap insurance, not a portability commitment.
- **Checked, no change:** the reserved-global-flag refusal and the `GIT_COMMAND_REFUSED` code are covered — five flags including `-c core.hooksPath=`, asserted in `repo-handle.test.ts`. Deleting `usesReservedGlobalFlag` fails five tests. Reported missing twice by a reviewer reading only the integration file.

- **Added:** tests for the config-injection defences, which were documented and unverified. A repo-local `core.fsmonitor` runs a program during `status`, and a global `diff.external` runs one during `diff`; both fire against unguarded git and are blocked here, each proven by removing the defence and watching the test fail.
- **Learned:** the first version of the global-config test was vacuous. It used `core.fsmonitor`, which the runner's `-c core.fsmonitor=` already overrides at every layer, so it passed with `GIT_CONFIG_GLOBAL` deleted — it was re-testing the previous defence. A test for one defence has to use a vector no other defence covers.
- **Added:** the first test that runs a mutating command against a real `ShadowRepo`. Nothing exercised the write path, so a guard that refused every repo rather than only user repos passed the whole suite.
- **Found:** `diff.external` is a repository-local execution vector alongside `textconv`, and the source comment named only the latter. Same residual risk and the same bound — `.git/config` is not cloned.

- **Fixed:** CI red on `dev`. The test asserting every allowlisted flag is a real git flag matched git's prose with a regex, and git changed the prose: 2.39 prints `-n, --dry-run` where 2.55 prints `-n, --[no-]dry-run`, so the literal `--all` never appears and four verbs failed on ubuntu while passing on the local 2.39. It now parses flag names out of the usage text and records `--[no-]x` under both spellings. Verified against the exact 2.55 output from the failed run, and re-checked that an invented flag still fails on both renderings.
- **Learned:** a test that shells out to a tool is pinned to that tool's _output format_, not just its behaviour. Parse tokens, never match the sentence around them.

## 2026-08-20

- **Fixed:** `isWithin` tested for a `..` prefix, so `relative()` returning `..bak` for a sibling _inside_ the directory read as an escape from it. An `indexFile` at `<repo>/..bak` passed the protection check and git would have written a stray file into the user's worktree. The escape has to be a whole `..` segment.
- **Fixed:** the runner never wrote to the child but left its stdin open, so `hash-object --stdin` and `update-index --stdin` blocked until the 30s timeout killed them — measured at 30,065 ms with the fix removed, 8 ms with it. The child now gets EOF immediately.
- **Decided:** a refusal is `GIT_COMMAND_REFUSED`, separate from `GIT_COMMAND_FAILED`. Both were one code distinguished only by `infra`, and the two mean different things to whoever is paged: a failure is the environment, a refusal is Interlock asking for something the safety rules forbid, which is a bug here.
- **Fixed:** the inherited-`GIT_*` filter compared case-sensitively. Windows matches variable names case-insensitively, so `git_dir` reaches git as `GIT_DIR`; the comparison is now case-insensitive on every platform.
- **Checked, no change:** an external `SIGTERM` is not misreported as a timeout. Node sets `error.killed` only when it issued the kill itself — measured `killed=false, signal=SIGTERM` for a signal sent by another process — so the `killed && SIGTERM` test already separates the two.

- **Fixed:** four ways the guard classified a write as safe, each verified against a real repo before and after. `read-tree -u` was `index-only` and overwrote an uncommitted edit — `-u` updates the working tree, which redirecting the index does not protect. `read-tree --index-output=<path>` was `index-only` and rewrote `.git/index`, because it overrides `GIT_INDEX_FILE` and so ignores the path the runner validated. `update-index --split-index` was `index-only` and left a `sharedindex.*` file in the user's `.git`, which lands in `$GIT_DIR` whatever the redirection says. `symbolic-ref -d <name>` was `read-only` and deleted the ref, because the positional-count heuristic sees the same one operand the reading form takes.
- **Decided:** flags are an **allowlist per verb**, not a list of dangerous ones. The first attempt at the fix was a denylist of the flags above, and it failed open within an hour of being written: git resolves any unambiguous prefix, so `--index-out=`, `--index=` and `--i=` are all `--index-output=`, and `--d` is `--delete` — all four verified rewriting the index or deleting a ref against real git while the guard called them safe. A denylist of flags fails exactly as the denylist of verbs did, and `--split-index` was found only by enumerating what each verb accepts. Long flags now match by prefix, which is safe only because git resolves a prefix to a flag it prefixes or to nothing; short flags match per character because git bundles them (`-um` is `-u`); scanning stops at `--` so a file named `-u` stays a path. Keyed by verb, since `-u` on `add` is `--update` and harmless while `-u` on `read-tree` writes the worktree.
- **Added:** a test that reads `git <verb> -h` and asserts every flag in the allowlist really exists. The prefix-matching argument holds only while each listed name is a real flag — an invented one licenses abbreviations of whatever else shares its prefix — and `read-tree --update`, which the first fix listed, turned out not to be a git flag at all.
- **Fixed:** `indexFile` validation compared normalised paths, which is not where a write lands. A symlink pointing at `.git` passed the check and rewrote the user's index, and so did an ordinary macOS path alias — `tmpdir()` returns `/var/folders/...` for a directory whose real path is `/private/var/folders/...`, so a handle and an `indexFile` could name the same file and compare as different. Both sides now resolve through `realpath`, the target via its parent since git has not created it yet, and an unresolvable path is refused rather than assumed external.
- **Decided:** flag checking covers the four verbs where a flag decides the class, not every flag of every allowed verb. `diff --output=<path>` writes a file and `grep -O` runs a program, but argv is built by Interlock and never supplied by a repository; the guard exists for a `kind` that did not survive a `JSON.parse`, not for attacker-chosen arguments. Recorded where the allowlist is defined so the boundary is deliberate.
- **Corrected:** `maxBufferBytes` counts bytes, not decoded characters. Measured on Node 26 — eight two-byte characters trip a limit of ten — because `execFile` measures with `Buffer.byteLength` when an encoding is set. The previous note claimed the opposite; the name was right and the comment was wrong.
- **Moved:** the index-redirection tests that used fictional paths. The guard now touches the filesystem, so only the cases decided on a path's shape stay unit tests and the rest run against a real repository.
- **Fixed:** an index redirection was checked against `rootPath` and `gitDir` only, which misses the git directory a linked worktree shares. A worktree's git dir is `<main>/.git/worktrees/<name>`, so its handle names neither the main checkout nor `<main>/.git`, and the index a redirection must miss lives in both. Branches under active edit sit in linked worktrees here, so this is the ordinary case.

## 2026-08-16 — 08-18

- **Done:** M1 repo discovery — `openUserRepo`, `describeRepo`, `listBranchRefs`, `mergeBase`. Covers the awkward states the task named: linked worktrees, detached HEAD, a worktree whose directory was deleted, an unborn HEAD, and a bare repository. 20 tests against real repositories; `discovery.ts` at 96.6% statements and 100% functions.
- **Found — a bug in the git runner, caught by using it:** `git worktree list` is read-only but `worktree` is in `MUTATING_GIT_COMMANDS`, so the guard refused the command discovery most needs. The guard now recognises read-only _verbs_ of mutating subcommands, but only when the verb immediately follows the subcommand. (**Superseded**: the denylist itself was later replaced by an allowlist, recorded above; the subcommand rule survived the change.) Flag-based read-only forms — `branch --list`, `config --get` — are deliberately not recognised, because a mutating flag can follow one in the same argv (`git branch --contains X -d Y`); callers wanting those use plumbing instead.
- **Decided:** two contract changes, both because the original signature could not be implemented honestly. `mergeBase` returns `string | null` — two branches grafted from different roots have no merge-base, and that is an ordinary answer rather than a failure. `listBranchRefs` takes the `RepoId` as an argument, because every `BranchRef` carries one and inventing a fresh repo id on each listing would be wrong.
- **Decided:** a bare repository is rejected with a new `REPO_BARE` code rather than `REPO_NOT_GIT`. It is a git repository; it just has no working tree, so nothing can be in flight. The distinction is what lets `doctor` and `init` say something useful.
- **Decided:** worktrees with a detached HEAD are skipped. They have no ref to name and every downstream consumer keys off one. Uncommitted work in a detached worktree is therefore invisible to Interlock — a real gap, recorded rather than hidden.
- **Decided:** `BranchRef` ids are minted per observation; the stable key is `(repoId, ref)`. Reconciliation belongs to the store, and the store task now says to upsert on the natural key or it will insert a duplicate row on every re-listing.
- **Found:** git reports fully-resolved paths, so on macOS a `/var/...` worktree comes back as `/private/var/...`. Any component comparing paths against discovery output must resolve its own side first; noted in the watcher task, where the mismatch would otherwise surface as a mystery.
- **Reviewed:** second git-runner review, and it found a real hole. The mutation guard was a **denylist of verbs**, which fails open: verified against a real repo that `read-tree --reset HEAD` destroyed a staged change and `update-ref` created a branch, neither being on the list, both through the one function whose stated job is to make that impossible. Replaced with an allowlist of read-only commands — an unfamiliar verb is now refused by default rather than permitted by default.
- **Decided:** index-writing commands (`add`, `read-tree`, `update-index`) are a third class, permitted against a `UserRepo` only when `indexFile` points outside the repository. That resolves a contradiction the review caught: `indexFile` existed for the snapshot task, but `add` was denied, so the capability could not be used for the one thing it was added for. The validation is what makes it safe — an `indexFile` inside `rootPath` or `gitDir` is refused, since pointing it at `.git/index` would write the very file the design protects.
- **Fixed:** the integration test had fabricated a `ShadowRepo` over the user repo's own path (`originPath` equal to `rootPath`) to get around that denial, so it passed in a configuration that cannot exist and hid the collision. It now stages a real `UserRepo`, which is the case that matters.
- **Fixed:** any signal death was reported as a timeout, sending anyone debugging an OOM kill after an elapsed limit that never elapsed. Only `killed` with `SIGTERM` — what `execFile` sends on timeout — is now reported as one.
- **Corrected:** the environment comment claimed a user's hooks and merge drivers could not change behaviour. `GIT_CONFIG_GLOBAL`/`SYSTEM` neutralise global and system config only; repository-local `.git/config` still applies, and the reviewer confirmed `core.fsmonitor` runs during `status` and `diff.<driver>.textconv` during `diff`. The comment now says what is true, and the runner passes `-c core.fsmonitor=` and `-c core.hooksPath=<devnull>`. `textconv` has no single key to clear and stays a documented residual risk, bounded because `.git/config` is not cloned.
- **Decided:** the runner is text-only. Output is decoded as UTF-8, so binary content and non-UTF-8 paths need a buffer-returning path, and `maxBufferBytes` counts decoded characters rather than bytes. Both are now stated where they bind rather than implied.

- **Reviewed:** git runner PR. Two real holes fixed, both mine. `GitRunOptions.env` was an open map applied after sanitisation, so a caller could set `GIT_DIR` and undo the isolation the runner exists to provide — replaced with a single `indexFile` capability. And the runner redacted `stdout`/`stderr` before returning, which would silently corrupt object ids and paths that happen to match a secret pattern, in output every downstream parser reads; output is now verbatim and redaction applies only to what is logged. Tests now use `vi.stubEnv` and `mkdtemp` rather than mutating `process.env` and building pid-suffixed paths.
- **Rejected from that review:** the claimed attached-short-flag bypass. Verified against git 2.39.3: `git -C/path` and `git -cfoo=bar` are both rejected as "unknown option" — a _global_ flag takes its value as a separate argument, so whole-argument matching is correct. Added a test recording this, since the assumption is plausible enough to be "fixed" wrongly later.
- **Also rejected:** that `GitRunner` carries a redundant overload. It has one signature with an optional third parameter, which is what the comment asked for.
- **Not applied:** upper-casing the `GIT_` environment filter for Windows case-insensitivity. Windows is out of scope, and on POSIX `Git_DIR` is a different variable that git never reads, so the change would be a no-op justified by a reason that is not true here.

- **Done:** M1 git runner. `execFile` with an argument array, `-C` rather than `process.chdir`, per-call timeout and buffer cap. Non-zero exit is returned as a result, not thrown — `merge-tree` reports conflicts that way. Throws only when the command could not run: timeout, missing or non-executable binary, buffer overflow, or a refused mutation. 22 tests, `repo-handle.ts` at 100% statements and functions.
- **Decided while building it:** inherited `GIT_*` environment variables are stripped rather than passed through, because a user's shell exporting `GIT_DIR` or `GIT_INDEX_FILE` would silently redirect every command the daemon runs. `GitRunOptions.env` was the escape hatch — **superseded**: it was replaced by the narrow `indexFile` capability, recorded above.
- **Decided while building it:** caller-supplied global flags that redirect git are refused — `-C`, `-c`, `--git-dir`, `--work-tree`, `--namespace`, `--exec-path`, `--config-env`. A `UserRepo` handle would otherwise be able to act on another repository, and `-c core.hooksPath=...` would run arbitrary code on the host. Flags of the same name after the subcommand are left alone, since `-c` means copy detection to `git log`.
- **Also set:** `LC_ALL=C` so porcelain output stays parseable regardless of locale, and `GIT_CONFIG_NOSYSTEM` alongside the devnull config paths.

- **Reviewed:** external review of M1, mostly accepted. Added: an active-load measurement (sustained CPU plus p50/p95 write-to-event latency under 60 s of continuous rewrites) because idle CPU only proves the watcher is not spinning and M2's budget needs the loaded number; scoped `add -A -- <paths>` seeded by `read-tree HEAD`, with whole-tree as the fallback; a last-tree-OID cache so an unchanged rewrite publishes nothing; a `branch.snapshot` event carrying `{branchRef, treeOid, changeSet}` so consumers key off content rather than filesystem noise; the `git gc` hazard on unreferenced snapshot objects; reflogs, `ORIG_HEAD` and `packed-refs` in the untouched test; a PID-plus-heartbeat mechanism for session reaping; and a test enforcing that only `repo-handle.ts` imports `child_process`.
- **Rejected from that review:** building `pairOverlap()` in M1. It is a pure function with no consumer until M2's scheduler, and CLAUDE.md forbids writing modules nothing calls. It lands in M2 with the code that uses it.
- **Corrected in that review:** the scoped-`add` suggestion omitted seeding the temp index. A scoped `add -A` into an empty index yields a tree containing only the changed paths — a silently corrupt snapshot rather than a slow one. `read-tree HEAD` first is mandatory, and M1 now says so.
- **Corrected in that review:** it proposed an ESLint rule for the single-git-call-site invariant. `eslint.config.js` already uses `no-restricted-imports` for layering, and flat config resolves a rule last-wins per file, so a new broad block would silently disable the layering checks it overlaps. A test is used instead.

- **Decided:** AgenticFlict is dropped. Nothing in the product depends on it, and it carries textual conflict labels only — so it could evaluate the one layer git already handles, not the semantic detection that is the differentiator. Golden fixtures plus OSS replay remain the evaluation, which the plan's own risk register already called self-sufficient. Removed from M0, M8, `evaluation.md`, `eval/README.md` and `.gitignore`.

- **Found:** agent-warning test, runs 1–2 of 5. Claude Code (Opus 5) on the archestra codebase, mid-task, given a true warning that a function it calls had been renamed by another session. Both runs **adapted**: verified the claim before acting, fixed import and call site, left the peer's file untouched, then resumed the original task. Run 2 narrated it — "let me verify that rename before changing the call site", then "confirmed, the rename landed in the working tree".
- **Implication:** the core bet has two positive data points. Agents verify before acting, so a false positive costs a file read rather than broken code; and they treat a peer's change as authoritative rather than reverting it, so Interlock only has to inform, not arbitrate.
- **Watch:** in run 2 the agent then moved to "check whether other call sites still reference the old name" — widening from its own file to the whole repository. In a real multi-agent setting that means editing files another branch owns. MCP payloads should scope the expected action to the caller's own work.
- **Caveat:** n=2, one agent, and the rename was visible in the same working tree. Real warnings describe changes in another worktree the agent cannot check. Behaviour on an unverifiable claim is still untested.

- **Measured:** pair cost on archestra (793,784 lines of backend TypeScript, 644 MB of history), git 2.39.3, medians.
  - `git merge-tree --write-tree` on a clean pair: **12.8 ms** (10 runs, 11.7–16.2). The merge is free at real scale — the Atlassian figure holds.
  - Materialising the merged tree into a scratch directory via `git archive | tar`: **1.62 s** (5 runs, 276 MB, 6,605 files). This is 127× the merge and is unavoidable before any semantic check.
  - Typecheck: **not measured on a real repo.** archestra has no `node_modules` installed, so the only available number is interlock itself at 2,781 lines — 1.09 s cold, 0.45 s warm — against a codebase 285× smaller. It says nothing useful about a mid-size project.
- **Implication:** the cost story is not "merge cheap, typecheck expensive". It is "merge free, _materialisation_ significant, typecheck unknown". Extracting the full tree per pair would dominate the budget on its own. M2's scratch worktree should update the previous tree by diff rather than re-extracting from scratch — that turns 1.6 s into roughly the size of the change.
- **Measured:** typecheck decomposition on interlock (2,781 lines, 1,073 dependency `.d.ts`), medians.
  - Floor, `tsc --version`: **436 ms**. Process start and compiler load, fixed regardless of project size.
  - Cold, no build info: **1084 ms** — so 648 ms of actual checking.
  - Warm, nothing changed: **443 ms**, i.e. the floor. Real work ≈ 0.
  - Warm, one file changed: **786 ms** — 350 ms of work. This is Interlock's real case.
- **Implication:** 40% of the small-repo number is fixed startup, which is why extrapolating by line count would have been badly wrong. Cold cost scales with dependency `.d.ts` volume and source size; incremental cost scales with the size of the change and its dependents, not the repository.
- **Estimate, not measured:** a cold `tsc` on a repo the size of archestra is likely 30 s – 3 min, which rules out cold-checking every pair continuously. A one-file incremental there is plausibly 2–10 s, which does not.
- **Design consequence:** continuous semantic checking is viable only if incremental state survives between checks. The scratch worktree must persist `.tsbuildinfo`, and the scheduler should prefer re-checking the same pair over rotating pairs, because every rotation throws away incremental state and pays the cold cost again. This contradicts the single shared scratch worktree recorded on 08-12 — M2 likely needs sticky per-pair scratch state or a small pool. Resolve before building the scheduler.

- **Decided (ADR-0005):** per-pair worktree pool replaces the single shared scratch worktree decided on 08-12. Small LRU pool, default 4, entered only after the overlap filter. Each check updates a slot by delta — `merge-tree --write-tree` → `commit-tree` → `reset --hard` — instead of extracting the tree, which turns the measured 1.62 s into the size of the change. `.tsbuildinfo` persists per slot; eviction is expensive and the scheduler must prefer hot pairs over rotating new ones.
- **Decided:** M3 hosts the TypeScript LanguageService in-process, one per hot pair, rooted at its pool slot — justified by the 436 ms spawn floor against 350 ms of real incremental work. **Caveat found while writing it up:** tsconfig `plugins` are loaded and executed by the language-service host, and the tsconfig comes from an agent-written merged tree, so plugins must be stripped from the resolved config or the daemon executes repository code and breaks hard rule 2 in the one place the sandbox does not cover.

- **Verified:** ADR-0005's load-bearing assumption holds. TypeScript keys `.tsbuildinfo` on content, not mtime — rewriting every source file with identical bytes costs 439 ms against 437 ms untouched and 1036 ms cold. A `reset --hard` therefore invalidates only genuinely changed files, which is what makes the pool's delta update worth doing.

- **Verified:** symlinked `node_modules` satisfies `tsc` from a tree at a different path — root and per-package links, clean build, correct emit. ADR-0005's other load-bearing assumption holds. Gotcha found doing it: `pnpm exec tsc` fails in the scratch tree because pnpm verifies dependencies first and tries to `pnpm install` there. The pool must invoke `node_modules/.bin/tsc` directly and never shell through a package manager.

- **Measured (the missing number):** typecheck cost on openselfservice — 85,224 lines of TypeScript across a 39-package turbo monorepo, npm workspaces. Medians.
  - `git merge-tree --write-tree`: **12.1 ms**, against archestra's 12.8 ms on a repo 9× larger. Merge cost is effectively constant — it tracks the diff, not the repository.
  - Materialise merged tree: **590 ms** (58 MB, 2,131 files), against archestra's 1.62 s (276 MB). Scales with tree size, sub-linearly.
  - Full cold build: **88.8 s** (38 of 39 packages; the Next.js app failed on skipped native install scripts).
  - Warm build, nothing changed: **1.85 s** — all cache hits.
  - **Warm build, one file changed in a mid-graph package: 12.28 s.** This is the realistic per-check cost.
  - Raw `tsc` once internal dependencies are built: 0.94 s for a 4.3k-line workspace, 2.31 s for an 11.3k-line one.
- **Found — this changes M3:** in a monorepo you cannot typecheck a merged tree by running `tsc`. Raw `tsc` fails with `TS2307: Cannot find module '@o2s/utils.logger'` because internal workspace packages resolve through their `dist/`, which does not exist until they are built. The semantic check is therefore "build the internal dependency graph, then typecheck" — 12.28 s incrementally, not the 0.94 s the compiler alone suggests. M3's toolchain detection has to discover and run the build orchestrator, not just find the typechecker.
- **Implication:** 12.28 s per check sits comfortably inside the stated 3-minute budget for a typecheck finding, but four hot pooled pairs is roughly 50 s of CPU per round, so pool size and check frequency are now tunable against a real number rather than a guess. Cold is 88.8 s here and would be far worse on a repo the size of archestra, which is the whole argument for the pool.
- **Design consequence:** the pool slot must persist the **build orchestrator's cache** as well as `.tsbuildinfo` — turbo's `.turbo` directory was 54 MB here, and losing it is the difference between 1.85 s and 88.8 s. Add it to ADR-0005's list of state that eviction destroys, and count it in the disk quota.

## 2026-08-12

- **Decided:** merges use `git merge-tree --write-tree` in the shadow object database, not a worktree per pair. Merged trees materialise into one reusable scratch worktree with `node_modules` symlinked from the user's checkout; a `package.json` or lockfile change routes that pair to a slow install path.
- **Decided:** `merge-tree` solves merge cost; the scheduler solves typecheck cost, and typecheck cost is the budget that matters. A semantic conflict is a merge that came out clean, so the merge step never filters those out — overlap does.
- **Decided:** the TypeScript compiler is the semantic detector. M3 gains a baseline differ (report only diagnostics absent from base, A and B alone) and an attribution engine (which branch caused which half). The hand-built rename and signature matchers are dropped.
- **Decided:** M4 becomes overlap pre-filter plus targeted tests. tree-sitter has no symbol table and cannot resolve names across files, so it pre-filters and never detects. Targeted tests stay — the compiler cannot see behavioural conflicts.
- **Decided:** false-positive rate is a tracked metric from M2. When unsure, say nothing.

## 2026-08-05

- **Done:** deleted the M3+ stub modules and the barrel/`exports` entries pointing at them; dashboard out of the workspace, solution tsconfig and Vitest projects until it declares react/vite; coverage threshold off; Dependabot docker entry removed; CONTRIBUTING and the issue/PR templates dropped.
- **Done:** git repo initialised on `main`. Licensing in place: `LICENSE_AGPL`, `LICENSE.md` router, `CLA.md` and its workflow.
- **Decided:** AGPL-3.0-only plus commercial terms (ADR-0002, accepted). `notImplemented` is for gaps inside a path being built now, not for pre-creating later milestones.
- **Done:** dependencies installed and **`pnpm verify` is green for the first time**. Fixes it took: `@eslint/js` was pinned to a version that does not exist; `@types/node` declared per package and `types: ["node"]` set, since nothing resolved Node globals; `ShadowRepo` imported from its owning module; ULID `bumpRandom` incremented bytes while the encoder reduced mod 32, so every 32nd id went backwards lexicographically — which the event-log replay order depends on; two redundant type assertions and an empty function body.
- **Done:** `tsconfig.check.json` added. Tests, `scripts/` and `eval/` were in no tsconfig, so they were neither typechecked nor type-aware linted; they are both now, and `format:check` joined `pnpm verify` to match CI.
- **Decided:** findings rank by severity, then confidence, then recency — severity no longer multiplies into confidence. A high-severity finding we are unsure of outranks a low-severity one we are certain of, because agents see only a handful of warnings per hour. `findingWeight` still gates the noise budget via `minWeight`.
- **Open:** `CLA.md` and `LICENSE.md` have unfilled entity/contact blanks and need review; CLA workflow needs a signature repo and `CLA_SIGNATURES_TOKEN`. tree-sitter vs the TypeScript compiler API wants an ADR before M4. `node:sqlite` likely closes ADR-0003. Coverage threshold is off until M2.
- **Decided:** `dev` is the integration branch and repository default; `main` is release-only and carries the tags. CI now runs on pushes to both. Recorded in `CONTRIBUTING.md` and `CLAUDE.md`.
- **Decided:** CLA signatures live on a `cla-signatures` branch of this repo rather than a separate one — outside contributors have no write access, so a maintainer merge is still the only way to change the record. Drops the second repo and the PAT.
- **Decided:** no company exists yet, so the CLA Entity is a natural person. `CLA.md` §8 assigns the granted rights to a successor entity, so incorporating later does not mean re-collecting signatures.
- **Next:** create the GitHub repo, push, set `dev` as default, add rulesets for both branches, then reproduce a textual and a semantic conflict by hand for `docs/demo/00-manual-conflict.md`.

## 2026-08-04

- **Done:** monorepo scaffold — workspaces, strict TS with project references, lint with layering rules, Vitest, CI; `shared` models and event vocabulary; event bus; docs set (architecture, evaluation, threat model); ADR-0001…0004.
- **Decided:** monorepo (ADR-0001); SQLite store (ADR-0003); security posture enforced by types and tests (ADR-0004).
- **Open:** license pending IP-policy confirmation (ADR-0002); native SQLite driver choice; AgenticFlict dataset not yet obtained.
- **Next:** install dependencies and get `pnpm verify` green; reproduce a textual and a semantic conflict by hand and write up `docs/demo/00-manual-conflict.md`.
